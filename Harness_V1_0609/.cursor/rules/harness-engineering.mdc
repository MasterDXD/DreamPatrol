---
description: Harness Engineering 多Agent框架核心规则
globs:
alwaysApply: true
---

﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿# Harness Engineering 多Agent框架 v2.34.0 — Cursor编辑器项目规则

> 版本：v2.34.0

## 项目概述
本项目基于 Harness Engineering 多Agent架构范式，采用"分层分责+文档驱动+流程管控+容错自愈"的工程化方法论。框架包含384+源文件、220+类、240+导出模块、242+ API端点、5325+测试用例。在Cursor编辑器中，你作为用户与AI协作，AI按照以下Agent角色和规则执行任务。

## 技术栈
- **运行时**：Node.js（CommonJS模块系统）
- **后端**：Node.js原生`http`模块（无Express/Koa），自研WebSocket处理器
- **前端**：原生HTML5 + CSS3 + Vanilla JavaScript（无React/Vue/Angular）
- **数据存储**：better-sqlite3
- **压缩**：brotli/gzip/deflate（zlib）
- **测试**：Node.js内置test runner + c8覆盖率
- **Lint**：ESLint（`npx eslint src/ test/ scripts/`，要求0 errors, 0 warnings）
- **PWA**：Service Worker + Web App Manifest

## 核心原则
- **分层分责**：Team Lead → Domain Analyst → Task Worker → QA，每层各司其职
- **文档驱动**：六层文档体系（架构→核心→模块→工具→深度→准则），双向链接形成知识图谱
- **流程管控**：六阶段执行流程（需求探索→需求分析→架构设计→模块开发→集成测试→部署上线）
- **容错自愈**：失败重试、任务重分配、Checkpoint恢复、模型降级
- **TDD强制**：先写测试后写代码，RED-GREEN-REFACTOR，无例外
- **证据验证**：声称完成必须提供实际证据，口头完成不算完成
- **自动路由**：Skill根据上下文自动触发，无需手动指定

## Agent角色体系（v2.7：6职能型 + 15任务型 + 7语言审查员 = 28个Agent）

### 职能型Agent（6个）
- **Team Lead（团队负责人）**：项目拆解、任务分配、进度监控、成果验收。编排者模式——从"个人贡献者"进化为"编排者"，设定方向、定义质量标准、审查AI产出，聚焦判断力而非执行力
- **Domain Analyst（领域分析师）**：需求分析、架构设计、代码审核、技术攻关
- **Task Worker（任务执行者）**：编码实现、工具调用、过程记录
- **Quality Assurance（质量保证）**：质量检查、测试设计、缺陷管理
- **DevOps Engineer（运维工程师）**：基础设施、构建部署、系统监控
- **Technical Writer（技术文档工程师）**：文档编写、知识管理、变更记录

### 任务型Agent（5个，v2.1新增）
- **Code Reviewer（代码审查员）**：专注代码质量审查，检查规范和反模式
- **Security Reviewer（安全审查员）**：专注安全审计，检测漏洞和合规性
- **Build Error Solver（构建错误解决者）**：专注编译错误、依赖冲突、CI/CD排查
- **Planner（实现规划师）**：专注需求探索、实现规划、任务拆解
- **Test Writer（测试编写者）**：专注TDD测试编写、覆盖率优化

### 语言专属审查员（5个，v2.7新增）
- **TypeScript Reviewer**：TypeScript代码质量审查
- **Python Reviewer**：Python代码质量审查
- **Go Reviewer**：Go代码质量审查
- **Rust Reviewer**：Rust代码质量审查+安全审计
- **Java Reviewer**：Java代码质量审查

### Codex方法论角色（1个人类角色，v2.7.101融合）
- **System Designer（系统设计者）**：人类工程师角色，负责设计AI Agent可靠工作的脚手架、定义边界、搭建反馈、将人类判断转化为机器规则。从"写代码的人"转变为"设计系统的人"。

## 执行流程

### 六阶段流程（Skill自动路由）
1. **需求探索**：brainstorming → Team Lead + Analyst → 设计方案文档
2. **需求分析与规划**：requirement-analysis → Team Lead + Analyst → 项目计划书、需求规格说明书
3. **架构设计**：architecture-design → Analyst → 系统架构图、模块划分、接口设计
4. **模块开发**：tdd-implement + module-development（TDD门禁）→ Worker + Analyst审核 → 源码、单元测试、文档
5. **集成测试**：integration-testing → QA + Analyst → 测试报告、缺陷报告
6. **部署上线**：verification-before-completion → deployment → DevOps + Lead → 部署文档、运维手册

### 单任务执行流程
1. 明确任务目标、交付物、验收标准
2. 自动匹配并激活相关Skill
3. 按Skill步骤执行任务（TDD强制：先测试后实现）
4. 执行verification-before-completion验证
5. 提供完成证据，提交审核

## 全局规则

### 任务执行
- 大任务拆分为6-9个批次，从宏观到微观逐步深入
- 每个阶段设置明确交付物和验收标准
- 未通过验收自动触发修正流程

### 上下文管理
- 优先加载当前任务相关的文档，避免信息过载
- 参考项目docs/目录下的文档体系
- 长对话中注意保留核心决策和状态
- Skill按需加载：仅加载当前阶段相关Skill的完整内容

### 文档规范
- 六层文档体系：架构→核心→模块→工具→深度→准则
- 命名规范：架构分析-xxx系统.md、核心功能-xxx流程.md、模块详解-xxx模块.md
- 使用[[文档名称]]创建双向链接

### 监控与容错
- 任务超时默认20分钟，失败重试3次（指数退避）
- 定期创建Checkpoint
- 模型不可用时降级到备用模型

### 成本控制
- Token预算：1,000,000,000
- 80%预警，95%切换低价模型，100%暂停
- 优先使用低价模型完成简单任务
- **令牌消耗自动记录**：每次对话轮次结束后，必须通过 `/api/token/record` 端点记录token消耗
  - 请求格式：`{ sessionId, tokens, inputTokens, outputTokens, toolCallTokens }`
  - 估算方法：输入字符数/4(英文)或/2(中文)；每次工具调用约200-500 token
  - 记录时机：Skill执行后、任务批次完成后、会话结束前

### 安全与权限
- 敏感信息加密存储，禁止日志泄露
- 文件删除、系统命令等敏感操作需用户确认
- 避免多Agent同时修改同一文件

### 最佳实践
- 上下文过长：触发压缩，按需加载
- 工具调用失败：检查参数和权限，重试或切换
- 任务超时：拆分为更小子任务
- 模型幻觉：增加事实核查步骤
- 并发冲突：使用文件级锁

## Skill体系（84个技能 = 20个验证技能 + 62个扩展技能 + 2个基础设施）

### Skill自动路由
你拥有以下技能（Skills），它们是强制性的工作流程，不是可选建议。在执行任何任务前，必须检查是否有相关技能应该被激活。

### 斜杠命令系统（24个命令）
用户可通过斜杠命令快速触发技能链：
- `/plan` → brainstorming → requirement-analysis → architecture-design
- `/code-review` → code-review
- `/security-review` → security-audit
- `/debug` → systematic-debugging → bug-fix
- `/fix` → bug-fix → verification-before-completion
- `/deploy` → verification-before-completion → deployment
- `/test` → integration-testing
- `/refactor` → refactor-code
- `/prompt` → ai-prompting
- `/audit` → security-audit
- `/build` → tdd-implement → module-development
- `/code-simplify` → refactor-code
- `/goal` → iterative-deepening → brainstorming → requirement-analysis
- `/learn` → iterative-deepening
- `/optimize` → performance-optimization
- `/review` → code-review → security-audit
- `/section` → ui-skills → design-md
- `/ship` → verification-before-completion → deployment
- `/spec` → requirement-analysis → architecture-design
- `/startup` → brainstorming → idea-validation → requirement-analysis → architecture-design → mvp-builder → deployment → ai-native-scaling
- `/decide` → decision-loop
- `/web` → web-interaction
- `/cli` → cli-anything
- `/research` → ai-research → source-driven-development

### 需求探索阶段
- direction-discovery.md — 方向发现（三条猎人路径：痛苦扫描+竞品空隙+技术红利/Product Lens三问过滤/痛点清单+竞品分析+一句话方向描述产出物）

### 需求分析阶段
- brainstorming.md — 头脑风暴（苏格拉底式提问需求探索）
- ai-research.md — AI自主调研（不确定实现细节时触发，产出结构化调研报告）
- decision-loop.md — 七层闭环决策（背景→选项→假设→反驳→标准→实验→复盘）
- idea-validation.md — 想法验证（验证创业想法或商业假设）
- requirement-analysis.md — 需求分析
- multi-agent-decision-guide.md — Multi-Agent决策指南（适用性门禁/避坑清单/架构选择决策树/推诿检测/黄金数据回归）

### 架构设计阶段
- architecture-design.md — 架构设计
- design-md.md — 设计语言文档（前端开发开始时触发）
- taste-skill.md — 审美判断（视觉质量评估/改进时触发，含用户路径分析）
- cloud-ai-blueprint.md — AI系统蓝图规划（AI系统架构规划/集成AI能力时触发）

### 模块开发阶段
- tdd-implement.md — TDD驱动开发（RED-GREEN-REFACTOR强制）
- module-development.md — 模块开发（TDD门禁）
- necessity-review.md — 必要性审查（新模块/抽象/依赖引入时触发）
- doc-completeness-rules.md — 文档完整性规则（需求规格/架构文档存在性检查，编码前文档先行强制）
- dispatching-parallel.md — 并行子代理调度
- code-review.md — 代码审查
- verification-before-completion.md — 完成前验证（强制证据）
- systematic-debugging.md — 系统化调试（4阶段）
- bug-fix.md — 缺陷修复
- security-audit.md — 安全审计
- performance-optimization.md — 性能优化
- refactor-code.md — 系统化重构
- ai-prompting.md — AI编程提示词工程（优化AI交互质量、防幻觉、提示词模板）
- iterative-deepening.md — 迭代深化推理（任务复杂度高/质量不达标时触发）
- multi-agent-fusion.md — 多Agent协同融合（多Agent协作任务时触发）
- impeccable.md — 设计规范落地（前端视觉质量改进时触发）
- ui-skills.md — 模块化UI工程（UI组件开发时触发）
- motion-ai-kit.md — 动效技能（动画/动效设计时触发）
- better-icons.md — 图标技能（图标集成/改进时触发）
- source-driven-development.md — 源码驱动开发（基于官方文档验证代码决策，防止AI幻觉和过时模式）
- deprecation-and-migration.md — 废弃与迁移管理（代码生命周期纪律，系统化移除旧代码）
- web-interaction.md — Web交互技能（OpenCLI集成，80+网站适配器，Chrome Bridge浏览器会话复用）
- mvp-builder.md — MVP构建（快速构建最小可行产品）
- code-locator.md — 精准代码定位（两阶段检索：关键词召回+依赖链扩展，BM25+AST调用图定位）
- cli-anything.md — 软件操控（CLI-Anything将任意软件转化为AI可调用CLI工具，与web-interaction互补）
- ai-delivery-optimization.md — AI交付优化（审查吞吐失衡检测/上下文漂移监控/"似对非对"代码识别/工作流重构模式/反馈循环优化）
- ooda-loop.md — OODA决策闭环（Observe-Orient-Decide-Act循环，态势感知/自适应决策/持续闭环迭代）
- optimization-loop.md — 优化循环求解器（无限循环迭代优化/可量化指标追踪/策略自动切换/快照回滚）
- hermes-rl.md — Hermes RL训练管线（强化学习训练/轨迹收集/奖励函数设计/训练运行管理）
- browser-use.md — BrowserUse浏览器自动化（AI Agent浏览器控制/CDP直连+MCP双模式/自愈工作流）
- claude-mem.md — 对话记忆持久化（5-Hook架构/3层渐进搜索/双持久化策略/会话恢复协议）
- data-collect.md — 多平台数据采集（三层采集体系OpenCLI/BrowserUse/CLI-Anything/平台-方案映射/模板化字段提取/6步执行流程）
- skill-distillation.md — 技能蒸馏（从高频执行路径提取可复用Skill/自动化技能精炼/模式识别与抽象）
- knowledge-base.md — 知识库管理（三层目录结构/raw→wiki→outputs管道/跨引用与Schema管理/结构化知识检索）
- clone-website.md — 网站克隆（CDP截图+DOM提取+样式还原/单页/多页克隆/资源下载与本地化）
- skill-effectiveness.md — 技能效能评估（技能使用频率/成功率/耗时追踪/效能评分/改进建议生成）

### 集成测试阶段
- integration-testing.md — 集成测试

### 部署上线阶段
- deployment.md — 部署上线（3步流程：准备→验证→部署）
- ai-native-scaling.md — AI原生规模化（产品规模化扩展或构建自动化运营系统）
- documentation.md — 文档编写
- auto-doc-generation.md — 自动文档生成（文档编写完成后触发）

### 扩展
- writing-skills.md — 技能编写（创建新Skill指导）

### 基础设施
- skill-router.md — Skill路由引擎（自动发现、匹配、激活）
- session-start-hook.md — 会话启动Hook（自动注入规则）

## 项目配置
详细配置参见 .harness/config.json，包含模型选择、Token预算、权限配置、Hook配置等。当前版本：2.7.170。

## 运行时引擎（v2.0引入，v2.7.170扩展）

### 架构：从Prompt工程到软件工程
v2.0引入可执行运行时引擎，将文档中的规则转化为可强制执行的代码，实现关键跨越。

### Agent子系统（src/runtime/agent/，15文件）
- **AgentRuntime** — Agent运行时核心。生命周期管理、状态转换、能力注册、attachPromptBuilder()提示词构建器注入、buildPrompt()提示词编译
- **AgentLifecycleController** — Agent生命周期控制器。创建→初始化→运行→暂停→恢复→终止
- **AgentMonitor** — Agent监控器。实时状态追踪、性能指标采集、异常检测、推诿检测（defer率分析+循环推诿DFS检测）
- **AgentDeployment** — Agent部署器。版本管理、灰度发布、回滚机制
- **AgentChannel** — Agent通信通道。消息路由、优先级队列、背压控制
- **AgentPackManager** — Agent包管理器。能力打包、依赖解析、版本兼容
- **AgentSandbox** — Agent沙箱。资源隔离、权限限制、超时保护
- **AgentStateManager** — Agent状态管理器。持久化、恢复、快照
- **AgentWorkflowIntegration** — Agent工作流集成。阶段绑定、任务分发、结果收集
- **MultiAgentRouter** — 多Agent路由器。负载均衡、亲和性调度、故障转移
- **SubagentExecutor** — 子Agent执行器。生成、取消、重试管理
- **AgentDebugLoop** — Agent自调试闭环。自主运行测试→分析失败→修复代码→回归测试→重新测试的完整自动化循环

### 因果子系统（src/runtime/causal/，9文件）
- **CausalDataBus** — 因果数据总线。事件发布/订阅、因果排序、版本追踪
- **CausalBufferManager** — 因果缓冲管理器。写入缓冲、刷盘策略、溢出处理
- **CausalConfigValidator** — 因果配置验证器。模式校验、依赖检查、一致性验证
- **CausalConsistencyChecker** — 因果一致性检查器。最终一致性验证、冲突检测
- **CausalMemoryStore** — 因果内存存储。内存索引、快速查询、TTL过期
- **CausalVectorIndex** — 因果向量索引。向量相似度搜索、嵌入管理
- **SimulationEngine** — 前向因果推演引擎。BFS分支构建、置信度衰减剪枝、反事实推理、因果链前向预测、模拟报告生成（融合自MiroFish预测引擎）
- **WorldLineManager** — 多世界线管理器。分支创建/合并（4种策略）、状态转移、概率连乘计算、置信区间、时间线报告（融合自MiroFish数字孪生）
- **ScenarioPredictor** — 场景预测器。蒙特卡洛模拟（3种采样方法）、概率分布输出、风险指标（VaR/ES/最大回撤）、敏感性分析、场景对比、预测报告（融合自MiroFish万物预测）

### 协作子系统（src/runtime/collaboration/，11文件）
- **CollaborationModeRouter** — 协作模式路由。solo/generator-verifier/orchestrator-subagent/agent-teams/message-bus/shared-state六种模式，Multi-Agent适用性决策门禁（3反模式+5正向信号+阈值推荐）
- **PairChat** — 配对对话。双Agent实时协作、轮流发言、共识达成。融合ChatDev交叉验证协议：双向/单向验证模式、幻觉检测与追踪（4级严重度）、验证标准检查、方向感知轮次统计（融合自ChatDev两两对话+交叉验证）
- **ChatChain** — 链式对话。多Agent顺序处理、上下文传递、结果聚合。融合ChatDev阶段产物追踪：产物注册与版本化（父子谱系）、跨阶段流转可视化、阶段有序产物检索（融合自ChatDev ChatChain工作流）
- **OutputFusion** — 输出融合。多Agent结果合并、冲突解决、质量加权
- **AgentDiversityManager** — Agent多样性管理器。角色/方法/错误/视角四维多样性评估，集成学习模式推荐（Bagging/Boosting）
- **EnsembleOrchestrator** — 集成编排器。Bagging（并行求稳）、Boosting（串行纠错）、Stacking（元学习）三种集成模式
- **AgentContributionTracker** — Agent贡献度追踪器。Agent权重/置信度记录，特征重要性输出，Top贡献者排名，集成模式分布统计
- **DevMetricsCollector** — 开发指标采集器。项目级全生命周期指标采集（时间/成本/文件数/幻觉纠正/Token用量）、阶段级分解统计、项目报告生成、全局统计（平均时长/成本/文件数/幻觉纠正数）、CJK感知Token估算、有界历史记录（融合自ChatDev效率与成本度量）
- **CrossMachineTransport** — 跨机器通信传输层。3种角色（coordinator/worker/peer）、9种消息类型（task-assign/task-result/heartbeat/resource-report/agent-register/agent-deregister/state-sync/broadcast/direct）、4种连接状态、HMAC-SHA256认证、心跳保活、自动重连、BoundedMap/BoundedArray内存限制
- **GroupChat** — AutoGen灵感多Agent群聊。4种发言者选择策略（round-robin/random/LLM-choice/custom），共享对话历史，暂停/恢复，终止条件，摘要函数，参与者管理
- **TaskBoard** — CrewAI灵感任务委派与认领。4种委派模式（manual/auto-assign/claim-based/bid-based），6种任务状态（open/claimed/in-progress/completed/failed/cancelled），能力注册，竞标提交，任务超时，历史追踪

### 上下文子系统（src/runtime/context/，6文件）
- **ContextCompressionEngine** — 上下文压缩引擎。智能分类（keep/summarize/discard）、Token估算、压缩计划
- **IsolatedContextManager** — 隔离上下文管理器。Agent间上下文隔离、共享边界控制
- **LtiContextInjector** — LTI上下文注入器。学习工具互操作上下文注入
- **PhaseContextInjector** — 阶段上下文注入器。根据执行阶段自动注入相关上下文
- **AutoregressiveContextSchema** — 自回归上下文模式。上下文自回归生成与验证
- **ThreeTierMemoryManager** — 三层分级记忆管理器。immediate（50K tokens，20条，5min保留）→ short-term（200K tokens，100条，1h保留）→ long-term（1.25M tokens，500条，无限保留），自动晋升/降级，重要性淘汰，1.5M总预算

### 深化推理子系统（src/runtime/deepening/，50+文件）
- **DeepeningOrchestrator** — 深化推理编排器。迭代精化、质量评分、收敛检测
- **DeepeningPipeline** — 深化管道。串联50+子模块（调度器/控制器/路由器/融合/缓存/熔断/限流等）
- **AdaptiveDepthController** — 自适应深度控制器。根据任务复杂度动态调整推理深度
- **ConvergenceDetector** — 收敛检测器。质量阈值判定、停滞检测、早停策略
- **IterativeRefinement** — 迭代精化器。多轮优化、增量改进、质量追踪
- **ProgressiveDeepening** — 渐进深化。从浅到深逐步推理
- **RecurrentDeepeningScheduler** — 循环深化调度器。周期性深化任务调度
- **TokenAwareDeepening** — Token感知深化。预算约束下的深化策略
- 其他模块：审计追踪、背压管理、基准测试、缓存、熔断器、配置管理、连接池、数据管道、依赖解析、部署、错误处理、事件总线/存储/回放、特性标志、优雅关闭、健康监控、负载均衡、锁管理、指标聚合/采集、模块注册、通知、插件系统、优先级队列、速率限制、报告生成、资源管理、重试策略、安全守卫、服务注册、快照存储/管理、状态机/管理、策略插件、任务队列/调度、节流、超时管理、验证器、可视化、工作流模板

### 基础设施子系统（src/runtime/infrastructure/，25文件）
- **SharedInfrastructure** — 共享基础设施。连接池/负载均衡/服务注册/特性标志
- **EventBus** — 事件总线。发布/订阅、事件过滤、优先级排序
- **EventRegistrar** — 事件注册器。自动发现事件处理器、生命周期绑定。含PairChat交叉验证事件（round-timeout/cross-validation-started/hallucination-detected/cross-validation-completed）、ChatChain产物事件（artifact-registered/artifact-versioned/task-retry）、DevMetricsCollector指标事件（project-started/project-completed/phase-started/phase-completed/hallucination-corrected），审计前缀覆盖dev-metrics:命名空间
- **MCPClient** — MCP协议客户端。stdio/HTTP双传输、进程退出清理、buffer限制、SSRF防护。支持OpenCLI集成（80+网站适配器，Chrome Bridge浏览器会话复用）和CLI-Anything集成（港大HKUDS开源，将任意软件转化为AI可调用CLI工具）。Dashboard API端点`/api/opencli/status`、`/api/opencli/servers`、`/api/cli-anything/status`、`/api/cli-anything/registry`提供运行时状态查询（含防御性编码：null/typeof检查、try-catch、返回数据过滤）
- **ConversationContextStore** — 对话上下文存储。多会话对话轮次持久化、自动压缩、摘要生成、SQLite WAL模式
- **AutoVersionTracker** — 自动版本追踪器。文件变更检测、版本号自动递增、变更日志生成
- **ModuleInitializer** — 模块初始化器。依赖排序、懒加载、初始化超时保护。SIMPLE_MODULES注册表含DevMetricsCollector，返回对象包含devMetricsCollector实例供事件注册器引用
- **ConcurrencyController** — 并发控制器。信号量、读写锁、并发限制
- **HealthChecker** — 健康检查器。liveness/readiness探针、依赖健康状态
- **HealthRegistrar** — 健康注册器。自动注册健康检查项
- **InstanceBuilder** — 实例构建器。工厂模式、依赖注入、单例管理
- **PlatformCoordinator** — 平台协调器。跨平台适配、环境检测
- **PluginManager** — 插件管理器。插件发现、加载、生命周期管理
- **PriorityQueue** — 优先级队列。堆实现、优先级调度
- **RetryEngine** — 重试引擎。指数退避、抖动、最大重试次数
- **SignalPersistence** — 信号持久化。进程信号处理、状态保存
- **SqliteStore** — SQLite存储。键值存储、事务支持、WAL模式
- **BrowserUseAdapter** — 浏览器使用适配器。CDP直连+MCP双模式、导航/截图/数据提取、DATA_COLLECT_TEMPLATES多平台采集模板（小红书/抖音/公众号/通用/Reddit/HackerNews/ProductHunt）、extractByTemplate模板化字段提取、_coerceTemplateTypes类型强制转换
- **CDPClient** — Chrome DevTools Protocol客户端。WebSocket直连Chrome CDP端点、命令发送、事件监听、帧掩码和缓冲管理
- **CodeGraph** — 代码依赖图。项目源码文件依赖关系图构建、JS require/exports和MD链接解析、依赖链查询、反向依赖查找、孤立文件检测、循环依赖发现
- **GitWorktreeManager** — Git工作树管理器。创建/移除隔离Git worktree供Agent并行工作、自动清理、超时回收、峰值统计
- **KnowledgeBasePipeline** — 知识库管道。三层目录结构管理（raw/wiki/outputs）、原始文档扫描、结构化Wiki条目生成、跨引用和Schema管理
- **RLTrainingPipeline** — 强化学习训练管线。训练运行生命周期管理（pending/running/completed/failed/stopped）、Atropos/Tinker训练服务对接、轨迹收集、奖励历史追踪、检查点管理
- **ServiceFS** — 服务虚拟文件系统。多服务适配器统一挂载到虚拟路径、标准化接口（list/read/write/remove/exists）访问各服务资源
- **SessionResumptionProtocol** — 会话恢复协议。多源上下文提取（对话/记忆/梦境/会话）、结构化恢复令牌生成、跨会话状态续接

### 模型子系统（src/runtime/model/，11文件）
- **TokenManager** — Token管理器。使用量追踪、预算检查、分类统计
- **ModelSelector** — 模型选择器。任务匹配、成本优化、降级策略。新增GPT-5.6模型（iris-alpha/beacon-alpha/ember-alpha，1.5M/800K上下文窗口），inferenceModes字段，getInferenceModeForTask()任务推理模式匹配，setPreferredTier()偏好层级设置
- **EmbeddingService** — 嵌入服务。文本向量化、相似度计算
- **TriAttention** — 三重注意力。Pre-RoPE空间三角级数评分+向量幅度双引擎注意力计算，自适应权重调整，离线校准Q/K聚类中心，关键信息聚焦
- **KVCacheManager** — KV缓存管理器。基于TriAttention校准的KV缓存压缩，三角级数距离偏好+向量幅度+时效性+访问频率四维评分剪枝，自适应权重调整，10倍+显存压缩
- **InferenceCache** — 推理缓存。相似Prompt结果复用，TTL过期，LRU淘汰，Token节省统计
- **InferenceModeManager** — 双推理模式管理器。xhigh（高精度，max 30s延迟，premium层级，L3技能加载）与fast（超低延迟<200ms，economy层级，L1技能加载）双模式，任务复杂度自动切换，手动覆盖，历史追踪，ModelSelector/SkillReducer集成
- **BatchScheduler** — 动态批处理调度器。优先级队列（CRITICAL/HIGH/NORMAL/LOW），请求类型分类（long-text/short-text/code/tool-call），类型亲和批处理，max 10请求/批次，完成回调分发
- **CostAwareRouter** — 成本感知路由器。analyzeComplexity()新增inferenceMode参数，xhigh→premium路由，fast→economy路由

### 质量子系统（src/runtime/quality/，14文件）
- **QualityScorer** — 质量评分器。多维度质量评估、阈值判定
- **SelfReflection** — 自反思。输出质量自评、改进建议生成，证伪维度反思（falsification）
- **AdversarialReview** — 对抗审查。故意寻找缺陷和漏洞，决策对抗模式（魔鬼代言人角色、证伪实验设计）
- **DocFreshnessGuard** — 文档新鲜度守卫。检测过时文档、提醒更新
- **SelfEvolutionGovernor** — 自演化治理器。控制自我改进的速率和范围
- **FeedbackCredibility** — 反馈可信度评估器。评估反馈来源可靠性、加权聚合
- **AiCodeTrustScorer** — AI代码可信度评估器。8项风险指标检测（含ALMOST_CORRECT"似对非对"模式），按来源追踪可信度趋势，指数衰减
- **ComprehensionDebtTracker** — 理解债务追踪器。5种债务类型×4级严重度×4种解决状态，债务评分/分布/解决率度量
- **DeliveryEfficiencyMeter** — 交付效率度量器。6阶段时间分布追踪，编码占比/AI加速比/审查瓶颈评分/审查吞吐失衡比率/管道瓶颈检测/反馈循环时长度量
- **ContextDriftMonitor** — 上下文漂移监控器。长任务约束快照、漂移检测（5级：none/low/medium/high/critical）、否定约束识别、漂移趋势追踪
- **AgentGoldenTest** — Agent黄金数据回归测试。黄金用例注册、输出比对（精确匹配/数值容差/字符串相似度/嵌套递归）、回归检测、历史追踪
- **AutoReinLearningLoop** — 自增强学习循环。连接反思→规则生成，5个内置规则模板（null-check/error-handling/resource-cleanup/unbounded-collection/hardcoded-delay），模式匹配→IronRuleEngine.addPatternRule()+ErrorPreventionGuard.registerErrorPattern()
- **PostTaskReviewer** — 任务后自动审查钩子。任务完成后触发：SelfReflection→AutoReinLearningLoop→MemoryPipeline→DreamBridge

### 会话子系统（src/runtime/session/，2文件）
- **SessionManager** — 会话状态管理。防抖持久化、TTL过期、阶段转换验证、Token预算检查、会话恢复
- **CheckpointManager** — 检查点管理器。定期快照、恢复点创建、增量保存

### 技能子系统（src/runtime/skill/，19文件）
- **SkillRouter** — Skill自动发现、匹配、路由。三层缓存（L1摘要/L2指令/L3资源），语义匹配，否定模式检测，内容去重，模型分级过滤（MODEL_TIERS: small/medium/large），Top-K技能选择，SkillReducer联动
- **SkillCreationEngine** — 技能创建引擎。从需求生成新Skill定义
- **SkillCurator** — 技能策展人。技能质量评估、冗余检测、推荐优化
- **SkillImprover** — 技能改进器。基于使用反馈优化技能
- **SkillImprovementLoop** — 技能改进循环。持续迭代优化，飞轮三道门验证（成功率阈值→回测→AB测试），蒸馏-eval闭环（飞轮promoted后自动触发重新蒸馏）
- **SkillGraph** — 技能图谱。技能关系建模、依赖可视化、影响范围分析
- **SkillEvolver** — 技能演化器。技能自动进化、变异生成、适应度评估、attachSkillCreationEngine()桥接newSkills输出到SkillCreationEngine、_processNewSkills()新技能自动注册
- **SkillReducer** — 技能精简器（动态技能管理系统）。合并相似技能、去除冗余；动态技能分层（核心/领域/基础设施），核心技能常驻L2；Top-K技能选择避免决策瘫痪；任务驱动激活与自动卸载（"按需加载，用完即藏"）；技能过载检测与注意力压缩
- **SkillDiscoverUtils** — 技能发现工具。文件扫描、元数据提取
- **PlaybookGenerator** — Playbook生成器。从DreamEngine模式自动生成结构化Playbook，支持版本追踪和持续精化，支持从人类工作流文档直接提取Playbook（generateFromHumanWorkflow）
- **SkillTreeDAG** — 技能树DAG。技能依赖图、拓扑排序执行、种子技能自生长、循环依赖检测
- **SkillCanary** — 技能金丝雀追踪。灰度发布、按比例放量、自动晋升/回滚、成功率与延迟评估（融合自Skills Refiner）
- **SkillObservability** — 技能统一可观察性。跨模块指标聚合、执行链路追踪、健康仪表盘、告警规则引擎（融合自Skills Refiner）
- **SkillAuditTrail** — 技能审计轨迹。变更记录、历史查询、影响分析、审计报告、持久化存储（融合自Skills Refiner）
- **SkillMemoryStore** — 技能记忆存储。Per-skill持久化经验存储，tips/avoidances/patterns按技能分类，相似技能间经验迁移，效果追踪与自动修剪
- **SkillRetirementManager** — 技能退休管理器。技能生命周期管理（active/retired/archived），退休条件评估（低效能/低频使用/被替代），reactivation重新激活，退休审计追踪
- **CodeWikiOrchestrator** — 代码Wiki编排器。5阶段编译管线（扫描→解析→索引→生成→导出），GraphifyCompiler+CodeGraph双源融合，Mermaid架构图生成，AI聊天式代码库查询，上下文文件自动生成

### Meta Skill子系统（src/runtime/skill/，3文件）
- **MetaSkillOrchestrator** — 元技能编排层。planChain(技能解析+拓扑排序+模型路由+Token预算分配)，executeChain/executeChainAsync(同步/异步执行+因果数据传递+并行执行+失败处理)，cancelChain，getChainStatus
- **CausalDataPasser** — 运行时因果数据传递。collectOutputs(收集causal_outputs)，injectInputs(注入causal_inputs)，validateCausalChain(验证数据完整性)，getData/clearData
- **MetaSkillGenerator** — 元技能自动生成。recordExecution(记录执行模式)，detectAndGenerate(检测高频组合→生成元技能定义)，getPatternStats，getGeneratedSkills，自动注册到SkillCreationEngine

### 思维子系统（src/runtime/thought/，23文件）
- **ThoughtExtractor** — 思维提取器。从对话中提取关键思维链
- **ThoughtDeduplicator** — 思维去重器。检测重复思维、合并相似推理
- **ThoughtMemoryStore** — 思维记忆存储。持久化思维链、快速检索
- **ThoughtRetrieverCycle** — 思维检索循环。迭代检索相关记忆
- **MemoryNudge** — 记忆推动。主动提醒相关历史决策
- **MemoryStore** — 记忆存储。长期记忆管理、遗忘曲线
- **DreamScheduler** — 做梦调度器。自动周期性触发DreamEngine回顾历史会话，会话结束时自动提炼经验，定时批量回顾
- **LlmWiki** — LLM知识库。结构化知识存储、领域知识检索、知识图谱构建
- **DreamEngine** — 做梦引擎。离线经验提炼、模式发现、知识整合
- **BrainMemory** — 脑记忆。分层记忆架构、工作记忆与长期记忆协调
- **MemoryPrefetcher** — 记忆预取器。5种预取信号（阶段变更/高熵/任务亲和/用户模式/周期），8组件attach，BoundedMap/BoundedArray容量限制，TTL淘汰，并发控制
- **UnifiedMemoryRecaller** — 统一记忆召回器。7源跨存储联合召回（BrainMemory/MemoryStore/ThoughtStore/LLMWiki/DreamEngine/CausalStore/Prefetcher），并行/顺序双模式，去重+置信度排序，查询缓存
- **MemorySyncCoordinator** — 记忆同步协调器。跨存储一致性协调，3种同步策略（事件/周期/手动），3种冲突解决（最新胜/置信度合并/源优先级），批量处理
- **MemoryPipeline** — 记忆管道集成。Hermes三阶段管道（Recall/Sync/Prefetch）统一接入，自动布线8+外部组件，单入口API，级联shutdown
- **DreamOutcomes** — Dreaming Outcomes闭环。成功标准定义、加权评分评估、效果追踪、反馈闭环（DreamEngine/SkillImprovementLoop双向同步）（融合自AI Agent Dreaming）
- **DreamBridge** — Dreaming模块桥接器。7条自动桥接规则（QualityScorer→Outcomes、SelfReflection→DreamEngine、Outcomes→DreamEngine/SkillImprovementLoop、DreamEngine→LlmWiki、SelfReflection→ErrorPreventionGuard、ErrorPreventionGuard→IronRuleEngine），事件驱动，内存泄漏防护
- **MemoryProviderInterface** — 外部记忆提供商接口。connect/disconnect/healthCheck/recall/write/query/delete/sync
- **ProviderRegistry** — 提供商注册表。注册/注销/健康检查/连接管理
- **ProviderAdapterBase** — 适配器基类。重试/超时/限流/错误分类/指标追踪
- **Mem0Adapter** — mem0适配器。云端/自托管记忆服务，REST API集成
- **HonchoAdapter** — Honcho适配器。AI原生记忆平台，用户级/会话级记忆管理
- **HindsightAdapter** — Hindsight适配器。本地部署记忆服务，记忆蒸馏
- **ProviderHealthChecker** — 提供商健康检查器。周期性探测+熔断器

### 用户子系统（src/runtime/user/，3文件）
- **UserModelManager** — 用户模型管理器。偏好学习、行为建模
- **AffinityLearner** — 亲和力学习器。用户-AI协作模式适配
- **StructuredIntent** — 结构化意图。自然语言意图解析、任务映射

### 生成子系统（src/runtime/generation/，1文件）
- **StructuredOutputGenerator** — 模板驱动结构化输出生成。7种输出格式（JSON/MARKDOWN/TABLE/FORM/REPORT/EMAIL/PLAN），4个内置模板（report/email/plan/json），输入验证，HTML消毒，输出验证回调

### TUI子系统（src/runtime/tui/，5文件）
- **TuiApp** — TUI终端应用。终端界面框架、输入处理、渲染循环
- **TuiOrchestrator** — TUI编排器。多面板布局、焦点管理、快捷键绑定
- **ReplEngine** — REPL引擎。交互式命令解析、历史记录、自动补全
- **PersonaManager** — 人格管理器。AI人格配置、角色切换、行为适配
- **QuickCommandRegistry** — 快速命令注册表。斜杠命令快捷注册、别名管理、模糊匹配

### 工作流子系统（src/runtime/workflow/，21文件）
- **PhaseOrchestrator** — 六阶段流程编排。定义阶段序列和转换规则，检查阶段完成条件，强制TDD门禁
- **CommandRouter** — 斜杠命令路由。自动发现.harness/commands/，支持别名、中文命令、模糊匹配
- **ProgrammableHookExecutor** — 可编程钩子执行器。10+内置处理器，顺序执行+阻塞中断，60s超时保护
- **GoalExecutor** — 目标执行器。自主迭代收敛，自动分解子任务，因果数据发布
- **PipelineExecutor** — 管道执行器。多步骤顺序执行、条件分支、并行扇出、attachPromptBuilder()提示词构建器注入、attachSecurityChain()安全链注入
- **WorkflowDag** — 工作流DAG。有向无环图、拓扑排序、依赖解析
- **WorkflowTemplate** — 工作流模板。预定义工作流、参数化、复用
- **HookHandlers** — 钩子处理器集合。10+内置处理器实现（含AI服务安全检查）
- **HumanApprovalGate** — 人工审批门。关键决策点人工确认
- **PlanPersistence** — 计划持久化。执行计划保存、恢复、版本追踪
- **RagPipeline** — RAG管道。文档检索、上下文增强、生成
- **OptimizationLoop** — 优化循环求解器。无限循环迭代优化、可量化指标追踪、MD优化日志、策略自动切换、快照回滚
- **GraphRag** — 图谱RAG。知识图谱检索、实体关系推理、多跳查询，支持Graphify集成向后兼容降级
- **BusinessGoal** — 业务目标。KPI追踪、目标分解、进度评估
- **SprintCycle** — 冲刺周期。迭代管理、回顾总结、速率追踪
- **ToolAdapter** — 工具适配器。外部工具封装、参数映射、结果转换
- **RiskApprovalGate** — 风险审批门。高风险操作审批、影响评估、回滚预案
- **ExecutionModeManager** — 执行模式管理器。模式切换、策略适配、上下文保持
- **AgentSkillsDiscipline** — 工程纪律插件。规范强制、反模式检测、质量门禁
- **PipelineAuditTrail** — 管道审计轨迹。步骤级审计追踪（createTrail/recordStep/completeTrail/rollbackTrail），步骤状态管理（pending/running/completed/failed/skipped/rolled-back），输入/输出捕获与截断，Mermaid流程图导出，步骤时间线
- **DistributedTaskScheduler** — 资源感知分布式任务调度器。4种调度策略（round-robin/least-loaded/resource-match/latency-aware）、7种任务状态（pending/dispatched/running/completed/failed/cancelled/timeout）、5种资源类型（cpu/gpu/memory/disk/network）、Worker注册/注销、任务重试、超时处理、Worker丢失恢复

### 图谱编译子系统（src/runtime/graphify/，7文件）
- **GraphifyCompiler** — 图谱编译器。7阶段管线（detect→ingest→build→cluster→analyze→report→export），增量编译，成本追踪，Manifest管理
- **FileTypeDetector** — 文件类型检测器。40+扩展名映射，批量检测，自定义映射
- **AstParser** — AST解析器。tree-sitter可选+regex降级，支持JS/TS/Python
- **SemanticExtractor** — 语义提取器。LLM语义提取，多模态支持，并行批处理，Token成本追踪
- **LouvainClusterer** — Louvain社区发现。两阶段模块度优化，层次聚类，可配置分辨率
- **GraphBuilder** — 图谱构建器。AST+语义合并，跨文件引用解析，边去重
- **GraphQueryEngine** — 图谱查询引擎。4种优化策略（分类索引/选择性优先/双向搜索/缓存物化），路径查找，子图提取，架构概览

### SDD规范驱动子系统（src/runtime/sdd/，4文件）
- **SddContractManager** — SDD合约管理器。四文档合约（propose→spec→design→tasks），阶段推进验证门禁，合约归档
- **IronRuleEngine** — 铁律引擎。10+内置铁律（跨层调用/直接DB/硬编码密钥等），自定义规则，违规追踪，规则启停，addPatternRule()模式规则生成，getRuleEffectiveness()/recordRuleOutcome()规则效果追踪
- **SddDocumentValidator** — 文档验证器。必需章节检查，12项质量门禁，文档模板，跨阶段一致性
- **SddPhaseBridge** — 阶段桥接器。SDD阶段→执行阶段映射，合约门禁强制执行

### 提示词编译子系统（src/runtime/prompt/，4文件）
- **PromptBuilder** — 提示词构建器。静态前缀+动态后缀分离，agent persona+rules+skills构建，SHA-256哈希缓存失效，attachPatternExtractor()模式提取器注入，支持PromptPatternExtractor学习性注入
- **PromptCacheManager** — 提示词缓存管理器。Anthropic cache_control标记，命中率统计，Token节省追踪
- **PromptPatternExtractor** — 提示词模式提取器。从执行历史提取prompt→result模式，推荐prompt injections，模式频率统计与效果评分，与PromptBuilder集成自动注入高效模式
- **PromptABTestFramework** — 提示词A/B测试框架。实验生命周期管理（draft/running/paused/completed/cancelled），Z-test统计显著性分析（90%/95%/99%置信度），显著时自动完成实验，均衡流量分配，5种指标类型（success-rate/quality-score/latency/token-usage/custom）

### 自主优化子系统（src/runtime/optimization/，1文件）
- **AutonomousOptimizationOrchestrator** — 自主优化编排器。6阶段闭环（OBSERVE→ANALYZE→HYPOTHESIZE→EXPERIMENT→EVALUATE→APPLY），4种优化目标（prompt/skill/workflow/strategy），组件挂载（skillImprovementLoop/dreamEngine/skillEvolver/promptABTest/effectivenessOptimizer），模式提取，洞察生成，假设检验，置信度阈值自动应用

### 安全编排子系统（src/security/，1文件）
- **ToolCallSecurityChain** — 工具调用安全链。9层安全链(3层级)，Auto≤10ms快速路径，AST代码注入检测，Prompt注入检测(R47)，人工审批门

### 安全检测子系统（src/runtime/security/，1文件）
- **InstructionFragmentDetector** — 指令碎片检测器。5种威胁模式（role-injection/data-exfiltration/code-execution/permission-escalation/indirect-injection），5级威胁等级（none/low/medium/high/critical），滑动窗口聚合+时间衰减，跨类别增强（+0.15），会话级威胁追踪

### 模式子系统（src/runtime/patterns/，5文件）
- **AgenticPatterns** — Google Agentic 8大核心模式增强。4个组件：ValidationGate（中间质量验证）、FallbackRouter（回退路由策略）、ConcurrencyController（并发控制）、ReflectionLoop（反思修订循环）
- **HarnessEngineering** — 驾驭工程6大约束增强。3个组件：ProgressiveDisclosureOrchestrator（渐进式上下文披露）、TripleDefenseCoordinator（三重防线协调）、VerificationLoop（自验证循环）
- **PlanExecuteEnhancer** — 先规划后执行双架构增强。4个组件：RollingPlanner（滚动规划器）、RequirementBoundary（需求边界锁）、PlanSchema（标准化规划输出）、PlanMemoryBridge（执行反馈学习桥）
- **MultiAgentControl** — 多智能体系统工程控制。4个组件：IterationGuard（迭代限制+熔断器）、ContextPipeline（上下文压缩管道）、DefinitionOfDone（量化完成标准）、OnionModelCoordinator（洋葱模型协调器）
- **ReActAgent** — 标准ReAct（Reasoning+Acting）Agent模式。think→act→observe迭代循环，自定义thinkFn/actFn/observeFn，工具注册，scratchpad（BoundedArray），toolResults（BoundedMap），收敛检测，超时处理，FINISH_KEYWORDS检测

### 权限执行引擎（src/permission/，3文件）
- **RBACEnforcer** — 基于角色的访问控制。strict/recommended/optional分级，Skill执行顺序验证
- **PermissionGuard** — 文件操作权限守卫。路径遍历防护，文件级锁，危险命令检测，确认过期机制
- **AuditLogger** — 审计日志。链式哈希完整性验证，防抖持久化，查询过滤

### TDD门禁执行器（src/gate/，16文件）
- **TDDGate** — TDD强制门禁。RED-GREEN-REFACTOR检测，覆盖率验证，抽象层级检查
- **EvidenceVerifier** — 证据验证器。技能证据类型定义，完成声明验证，自反思提示，成功标准强度分类（强/弱），弱标准需人工确认
- **FrameworkComplianceChecker** — 框架合规检查器。命名规范/结构规则/安全规则/Karpathy规则（含孤立代码检测、可追溯性检查）/设计规则，SDD合约完整性检查（attachSddContractManager/checkSddContractComplete）
- **DesignSkillEngine** — 设计技能引擎。反模式检测、5→15大设计语言预设、6维度加权审美评分、无障碍审计（9规则）、动效性能检测、布局反模式检测、量化标准检测、polish自动修复（6类）、harden健壮化
- **CodeReviewFrameworkCheck** — 代码审查框架。自动检查清单，命名/结构/安全/持久化/API/测试/文档合规
- **DeviationApproval** — 偏差审批。请求/批准/拒绝/撤销生命周期，TTL过期
- **GeneratorVerifier** — 生成器验证器。5维度评估（逻辑/需求/边界/一致性/完整性），支持记忆-代码一致性检查
- **SharedRuleHelpers** — 共享规则辅助。规则加载、缓存、合并工具函数
- **SkillPatchApproval** — 技能补丁审批。技能修改审批流程、影响评估
- **DesignTokens** — 设计令牌。排版/间距/色彩/动效/响应式/组件/微交互/无障碍/交互状态/方差/图标/15家公司设计语言/量化标准
- **LayerBoundaryGuard** — 层级边界守卫。四层架构（Interaction/Business/Domain/Infrastructure）依赖方向强制，跨层违规检测
- **ArchitectureBoundaryEnforcer** — 架构边界执行器。模块间依赖方向约束矩阵，strict/recommended/optional分级
- **CodeDriftDetector** — 代码漂移检测器。跨时间架构偏离追踪，违规增长/耦合度/层失衡告警
- **ErrorPreventionGuard** — 错误预防守卫。全局错误模式注册表，执行前自动检查历史错误模式并注入警告，与DreamEngine闭环集成，Reflexion持久化（结构化失败模式记录与上下文注入），autoRegisterFromReflection()自反思自动注册
- **OutputConcisenessGuard** — 输出精简度守卫。Token/行数/重复率/填充词/注释比五维检测，冗长惩罚机制
- **KarpathyEnhancer** — Karpathy原则增强器。代码简洁性检查、可读性评分、反模式检测
- **DOC_COMPLETENESS_RULES** — 文档完整性规则。需求规格/架构文档存在性检查，编码前文档先行强制

### 工具层（src/utils/，30文件）
- **constants** — 全局常量定义
- **config-validator** — 配置验证器。版本白名单、结构校验、敏感信息检测
- **debounced-persister** — 防抖持久化。批量写入、防抖间隔、崩溃恢复
- **debug-logger** — 调试日志器。分级日志、性能追踪、敏感信息过滤
- **fs-utils** — 文件系统工具。JSON加载、Markdown扫描、安全路径
- **safe-parse/safe-execute/safe-assign** — 安全执行工具。异常捕获、默认值、深拷贝
- **sanitizer** — 消毒器。XSS防护、HTML净化、SQL注入防护
- **structured-logger** — 结构化日志。JSON格式、上下文注入、级别控制
- **lru-cache/ttl-cache/bounded-map/bounded-array** — 缓存与集合。LRU淘汰、TTL过期、容量限制
- **ring-buffer/min-heap/priority-queue** — 数据结构。环形缓冲、最小堆、优先队列
- **unique-id/stable-stringify/deep-clone/state-compare** — 通用工具。ID生成、稳定序列化、深比较
- **param-validator/path-utils/network-utils** — 验证工具。参数校验、路径处理、网络检测
- **i18n** — 国际化。多语言支持、翻译管理
- **shutdown-mixin** — 关闭混入。优雅关闭、资源清理
- **json-store-restorer** — JSON存储恢复。损坏检测、自动修复
- **keyed-debouncer** — 键控防抖器。按键防抖、批量执行
- **capacity-config** — 容量配置。资源限制、配额管理

### Web仪表盘（src/web/）
- **server.js** — 核心HTTP服务器。242+ API端点、CSP nonce、速率限制
- **websocket-handler.js** — WebSocket实时通信。RFC 6455实现、心跳、认证
- **static-file-server.js** — 静态文件服务。ETag缓存、Range请求、MIME检测
- **compression.js** — HTTP压缩。brotli > gzip > deflate优先级
- **changelog-archive.js** — 更新日志归档
- **dashboard/** — Dashboard后端模块（路由/数据提供/中间件/常量/解析器/验证）
- **public/** — 前端SPA（index.html + styles.css + app.js + sw.js + manifest + 图标）

### 使用方式
```javascript
const SkillRouter = require('./src/runtime/skill/skill-router');
const SessionManager = require('./src/runtime/session/session-manager');
const RBACEnforcer = require('./src/permission/rbac-enforcer');
const TDDGate = require('./src/gate/tdd-gate');
const PairChat = require('./src/runtime/collaboration/pair-chat');
const ChatChain = require('./src/runtime/collaboration/chat-chain');
const DevMetricsCollector = require('./src/runtime/collaboration/dev-metrics-collector');

const router = new SkillRouter(projectRoot);
router.discover();
const matches = router.match({ userMessage, agent, completedSkills });

const enforcer = new RBACEnforcer(projectRoot);
enforcer.load();
if (!enforcer.canExecute(agent, skillId)) { /* 阻止执行 */ }

const pairChat = new PairChat();
const { sessionId } = pairChat.startCrossValidation({
  agentA: 'programmer', agentB: 'reviewer',
  artifact: 'src/module.js', artifactType: 'code',
  mode: PairChat.CROSS_VALIDATION_MODES.BIDIRECTIONAL,
  validationCriteria: [
    { id: 'logic', description: '逻辑正确性', required: true },
    { id: 'security', description: '安全性', required: true },
  ],
});
pairChat.addCrossValidationRound(sessionId, {
  from: 'programmer', to: 'reviewer',
  hallucinationCorrections: [{ severity: PairChat.HALLUCINATION_SEVERITY.MEDIUM, description: '空指针未检查' }],
  criteriaResults: [{ id: 'logic', passed: true }, { id: 'security', passed: false }],
});
const report = pairChat.getCrossValidationReport(sessionId);

const chatChain = new ChatChain();
const chainId = chatChain.createChain({ name: '软件开发流水线' }).chainId;
chatChain.registerArtifact(chainId, { name: 'design-doc', type: 'document', phase: 'design', content: '...' });
chatChain.registerArtifact(chainId, { name: 'source-code', type: 'code', phase: 'coding', content: '...' });
const flow = chatChain.getArtifactFlow(chainId);

const metrics = new DevMetricsCollector();
const projectId = metrics.startProject({ name: 'ChatDev融合', agentCount: 4 }).projectId;
metrics.startPhase(projectId, 'design');
metrics.completePhase(projectId, 'design', { filesProduced: 3 });
metrics.recordHallucinationCorrection(projectId, 'coding', 2);
metrics.completeProject(projectId);
const projectReport = metrics.generateReport(projectId);
```

### 验证命令
- `npm test` — 运行5325+单元测试和集成测试
- `npm run validate` — 框架一致性检查 + 测试
- `npx eslint src/ test/ scripts/` — ESLint代码质量检查（0 errors, 0 warnings）
- `node harness-cli.js validate` — CLI快速验证
- `node harness-cli.js commands` — 列出所有24个斜杠命令
- `node harness-cli.js skills` — 列出所有84个技能及验证状态
- `node harness-cli.js memory-verify` — 验证记忆-代码一致性
- `node harness-cli.js antipattern-detect` — 运行反模式检测
- `node harness-cli.js dashboard` — 启动监控仪表盘（默认端口3210）
- `node harness-cli.js dashboard --open` — 启动并自动打开浏览器
- `node harness-cli.js dashboard --port <number>` — 指定端口启动

## KEPA自学习闭环（v2.7.170引入）

KEPA（Knowledge-Evolution-Pattern-Adaptation）自学习闭环是v2.7.170引入的三环自学习架构，通过"技能演化→模式提取→生命周期管理"三个闭环实现框架从经验中持续自动学习和自我完善的能力。

### 三环架构

| 闭环 | 优先级 | 核心模块 | 功能 |
|------|--------|---------|------|
| P0: SkillEvolver Bridge | P0 | SkillEvolver + SkillCreationEngine | 技能演化闭环：SkillEvolver通过attachSkillCreationEngine()桥接newSkills输出到SkillCreationEngine，_processNewSkills()自动注册演化产生的新技能 |
| P1: PromptPatternExtractor | P1 | PromptPatternExtractor + PromptBuilder | 模式提取闭环：从执行历史提取prompt→result高效模式，通过PromptBuilder.attachPatternExtractor()注入，自动推荐和注入高效提示词模式 |
| P2: SkillRetirementManager | P2 | SkillRetirementManager | 生命周期闭环：技能生命周期管理（active→retired→archived），退休条件评估（低效能/低频使用/被替代），reactivation重新激活，退休审计追踪 |

### 闭环流程

```
执行历史 → PromptPatternExtractor(模式提取) → PromptBuilder(高效模式注入)
    → SkillEvolver(技能演化) → _processNewSkills() → SkillCreationEngine(新技能注册)
    → SkillRetirementManager(生命周期管理) → 退休/归档/重新激活
    → 反馈闭环 → 执行历史更新
```

## Auto Research 自主优化闭环子系统

Auto Research自主优化闭环是统一自主优化子系统，通过AutonomousOptimizationOrchestrator编排5个核心组件，实现"观察→分析→假设→实验→评估→应用"六阶段闭环的持续自主优化能力。

### 5组件覆盖表

| 组件 | 子系统 | 核心能力 |
|------|--------|---------|
| SkillImprovementLoop | skill | 技能改进循环：持续迭代优化，飞轮三道门验证（成功率阈值→回测→AB测试），蒸馏-eval闭环 |
| DreamEngine | thought | 做梦引擎：离线经验提炼、模式发现、知识整合 |
| SkillEvolver | skill | 技能演化器：技能自动进化、变异生成、适应度评估、attachSkillCreationEngine()桥接新技能注册 |
| PromptABTestFramework | prompt | 提示词A/B测试：实验生命周期管理（draft/running/paused/completed/cancelled），Z-test统计显著性分析（90%/95%/99%置信度），5种指标类型（success-rate/quality-score/latency/token-usage/custom） |
| EffectivenessOptimizer | optimization | 效能优化器：效能评估与优化策略执行 |

### Auto Research闭环流程

```
观察数据 → OBSERVE(指标采集+上下文记录)
    → ANALYZE(模式提取+洞察生成)
    → HYPOTHESIZE(假设生成+置信度评估)
    → EXPERIMENT(AB测试+组件挂载)
        → PromptABTestFramework(prompt优化实验)
        → SkillImprovementLoop(skill改进实验)
        → DreamEngine(模式发现实验)
        → SkillEvolver(技能演化实验)
        → EffectivenessOptimizer(效能优化实验)
    → EVALUATE(统计显著性分析+胜者判定)
    → APPLY(置信度阈值自动应用+优化落地)
    → 反馈闭环 → 观察数据更新
```

## GPT-5.6 大上下文适配子系统（v2.7.170融合）

GPT-5.6（iris-alpha）融合引入的大上下文适配子系统，通过"双推理模式→三层记忆→批处理调度→指令安全"四模块协同架构，实现1.5M上下文窗口的高效利用和安全防护。

### 模块清单

| 模块 | 文件 | 子系统 | 核心能力 |
|------|------|--------|---------|
| InferenceModeManager | inference-mode-manager.js | model | 双推理模式管理：xhigh（高精度，max 30s延迟，premium层级，L3技能加载）与fast（超低延迟<200ms，economy层级，L1技能加载），任务复杂度自动切换，手动覆盖，历史追踪，ModelSelector/SkillReducer集成 |
| ThreeTierMemoryManager | three-tier-memory-manager.js | context | 三层分级记忆架构：immediate（50K tokens，20条，5min保留）→ short-term（200K tokens，100条，1h保留）→ long-term（1.25M tokens，500条，无限保留），自动晋升/降级，重要性淘汰，1.5M总预算 |
| BatchScheduler | batch-scheduler.js | model | 动态批处理调度：优先级队列（CRITICAL/HIGH/NORMAL/LOW），请求类型分类（long-text/short-text/code/tool-call），类型亲和批处理，max 10请求/批次，完成回调分发 |
| InstructionFragmentDetector | instruction-fragment-detector.js | security | 指令碎片攻击检测：5种威胁模式（role-injection/data-exfiltration/code-execution/permission-escalation/indirect-injection），5级威胁等级（none/low/medium/high/critical），滑动窗口聚合+时间衰减，跨类别增强（+0.15），会话级威胁追踪 |

### 增强模块

| 模块 | 新增能力 |
|------|---------|
| ModelSelector | 3个GPT-5.6模型条目（iris-alpha/beacon-alpha/ember-alpha，1.5M/800K上下文窗口），inferenceModes字段，getInferenceModeForTask()任务推理模式匹配，setPreferredTier()偏好层级设置 |
| CostAwareRouter | analyzeComplexity()新增inferenceMode参数，xhigh→premium路由，fast→economy路由 |

## Google Cloud 5 Agent Skill Design Patterns 适配子系统

基于Google Cloud 5 Agent Skill Design Patterns，框架已实现全部5种模式的适配覆盖：

| 模式 | 名称 | 覆盖模块 | 子系统 |
|------|------|---------|--------|
| Pattern #1 | Router（路由器） | SkillRouter | skill |
| Pattern #2 | Generator（生成器） | StructuredOutputGenerator | generation |
| Pattern #3 | Orchestrator（编排器） | PhaseOrchestrator, DeepeningOrchestrator, MetaSkillOrchestrator | workflow/deepening/skill |
| Pattern #4 | Agent（自主代理） | AgentRuntime, AgentLifecycleController, GoalExecutor | agent/workflow |
| Pattern #5 | Pipeline（管道） | PipelineExecutor, PipelineAuditTrail | workflow |

### 模式-模块映射流程

```
用户请求 → SkillRouter(Pattern #1: Router路由匹配)
    → StructuredOutputGenerator(Pattern #2: Generator结构化输出)
    → PhaseOrchestrator/DeepeningOrchestrator(Pattern #3: Orchestrator编排)
    → AgentRuntime/GoalExecutor(Pattern #4: Agent自主执行)
    → PipelineExecutor/PipelineAuditTrail(Pattern #5: Pipeline管道追踪)
```

## Google Agentic Design Patterns 8大核心模式增强（v2.30.0融合）

基于Google《Agentic Design Patterns》（Antonio Gulli著），框架在原有5种Cloud模式基础上，新增8大核心模式的增强实现。核心增强模块 `AgenticPatterns`（`src/runtime/patterns/agentic-patterns.js`）包含4个组件：

- **ValidationGate** — 链式步骤中间质量验证，防止低质量输出传播
- **FallbackRouter** — 路由无匹配时的回退策略（宽松匹配→注册处理器→澄清请求）
- **ConcurrencyController** — 信号量模式并发控制 + 指数退避重试
- **ReflectionLoop** — 反思-修订循环，收敛检测 + rollback-and-revise触发重新规划

8大模式完成度：Prompt Chaining 85% | Routing 70% | Parallelization 60% | Reflection 65% | Tool Use 45% | Planning 55% | Multi-Agent 60% | Memory 55%

详细架构分析见：`docs/architecture/架构分析-Agentic设计模式.md`

## 驾驭工程（Harness Engineering）六大架构约束增强（v2.31.0融合）

基于Harness Engineering理念，框架新增6大架构约束的增强实现。核心增强模块 `HarnessEngineering`（`src/runtime/patterns/harness-engineering.js`）包含3个组件：

- **ProgressiveDisclosureOrchestrator** — 渐进式上下文披露编排，按阶段控制上下文披露粒度
- **TripleDefenseCoordinator** — 三重防线协调（软约束→硬编码→Linter），自动检测覆盖缺口
- **VerificationLoop** — 自验证循环，前置验证→执行→后置验证→修复循环

6大约束完成度：渐进式披露 75% | 三重防线 60% | 自验证循环 70% | 上下文隔离 65% | 熵治理 70% | 可拆卸性 55%

核心原则：**解决已经出现的，而不是预防未来的错误**

详细架构分析见：`docs/architecture/架构分析-驾驭工程约束.md`

## 先规划后执行（Plan-then-Execute）双架构增强（v2.32.0融合）

基于"先规划后执行"方法论，框架新增规划-执行双架构增强实现。核心增强模块 `PlanExecuteEnhancer`（`src/runtime/patterns/plan-execute-enhancer.js`）包含4个组件：

- **RollingPlanner** — 滚动规划器，支持小步规划+快速执行（planningHorizon默认3步）
- **RequirementBoundary** — 需求边界锁，显式定义范围内外+边界锁定机制
- **PlanSchema** — 标准化规划输出格式，P0/P1/P2优先级+risks/resources字段
- **PlanMemoryBridge** — 执行反馈学习桥，从历史执行结果中提取规划模式

核心方法论：**规划层3步（需求解析→步骤拆分→风险预判）+ 执行层2步（分步反馈→动态调整）**

详细架构分析见：`docs/architecture/架构分析-先规划后执行.md`

## 多智能体系统工程控制（v2.33.0融合）

基于多智能体系统工程落地三大控制机制，框架新增统一控制系统。核心增强模块 `MultiAgentControl`（`src/runtime/patterns/multi-agent-control.js`）包含4个组件：

- **IterationGuard** — 统一迭代限制+熔断器框架，closed→open→half-open三态熔断
- **ContextPipeline** — Agent间上下文压缩管道，三层Token分配+结构化摘要
- **DefinitionOfDone** — 量化完成标准+4类终止条件(DoD满足/预算耗尽/无进展/边界越限)
- **OnionModelCoordinator** — 洋葱模型协调器，核心层→中间层→外层(人工接管)

核心方法论：**流程控制 + Token控制 + 完成标准(4类终止条件)**

详细架构分析见：`docs/architecture/架构分析-多智能体系统工程控制.md`

## 多Agent协作框架选型融合（v2.34.0融合）

基于6大多Agent协作框架设计模式，框架实现了全覆盖的协作模式适配，通过"单Agent推理→多Agent对话→任务委派"三层架构实现从简单到复杂的全场景协作能力。

### 6大模式覆盖表

| 模式 | 来源框架 | 覆盖模块 | 子系统 | 核心能力 |
|------|---------|---------|--------|---------|
| WAT（Write-Audit-Test） | ChatDev | PairChat, ChatChain | collaboration | 结对编程+交叉验证+链式对话+阶段产物追踪 |
| LangChain-Graph | LangGraph | AgenticPatterns, PlanExecuteEnhancer | patterns | 提示链+路由+并行化+反思循环+规划-执行双架构 |
| ReAct（Reasoning+Acting） | ReAct | ReActAgent | patterns | think→act→observe迭代循环+工具注册+收敛检测 |
| Plan&Execute | Plan-and-Execute | PlanExecuteEnhancer, GoalExecutor | patterns/workflow | 滚动规划+需求边界锁+执行反馈学习桥 |
| AutoGen | AutoGen | GroupChat | collaboration | 多Agent群聊+4种发言者选择策略+终止条件+摘要 |
| CrewAI | CrewAI | TaskBoard | collaboration | 任务委派与认领+4种委派模式+6种任务状态+竞标 |

## 屎山代码止血机制（v2.7.170融合）

基于Vibe Coding反模式分析，框架集成了四条"止血"机制，防止代码膨胀到不可维护：

### 命名红线（NO_VAGUE_FILENAME）
- FrameworkComplianceChecker新增`NO_VAGUE_FILENAME`规则（ERROR级别）
- 检测文件名中的模糊后缀：v2/v3/final/new/old/backup/copy/tmp/temp/draft/wip等
- `checkVagueFilename()`函数在shared-rule-helpers.js中实现
- 命名红线：文件名必须清晰说明功能，禁止`utils-v3-final.js`等模糊命名

### Dead Code定期清理（/cleanup命令）
- CodeGraph新增`detectUnusedExports()`方法：符号级dead code检测
- 新增`/cleanup`斜杠命令：检测孤立文件+未使用导出+模糊文件名
- 建议每周五执行`/cleanup`，定期清理无用代码
- 安全规则：仅报告不自动删除，每项需人工确认

### 架构图先行（exportMermaid）
- GraphifyCompiler新增`exportMermaid()`方法：将代码图谱导出为Mermaid流程图
- doc-completeness门禁已强制要求架构文档先行
- 开发前必须先画架构图，再让AI写代码

### Commit纪律（commit-discipline规则）
- 新增`.harness/rules/commit-discipline.md`规则文件
- 每天最多5个有意义的commit
- 使用Conventional Commits格式：`type(scope): description`
- 提交前必须运行ESLint检查

## 多Agent跨机器交互子系统（OpenClaw融合）

基于OpenClaw跨机器交互理念，框架新增多Agent跨机器交互子系统，通过"传输层→调度层→协作层→资源层→安全层"五组件协同架构，实现多Agent跨机器的高效通信、资源感知调度和可靠协作。

### 5组件覆盖表

| 组件 | 模块 | 子系统 | 核心能力 |
|------|------|--------|---------|
| 传输层 | CrossMachineTransport | collaboration | 跨机器通信传输：3种角色（coordinator/worker/peer）、9种消息类型（task-assign/task-result/heartbeat/resource-report/agent-register/agent-deregister/state-sync/broadcast/direct）、4种连接状态、HMAC-SHA256认证、心跳保活、自动重连、BoundedMap/BoundedArray内存限制 |
| 调度层 | DistributedTaskScheduler | workflow | 资源感知分布式任务调度：4种调度策略（round-robin/least-loaded/resource-match/latency-aware）、7种任务状态（pending/dispatched/running/completed/failed/cancelled/timeout）、5种资源类型（cpu/gpu/memory/disk/network）、Worker注册/注销、任务重试、超时处理、Worker丢失恢复 |
| 协作层 | CollaborationModeRouter | collaboration | 协作模式路由：6种模式（solo/generator-verifier/orchestrator-subagent/agent-teams/message-bus/shared-state），Multi-Agent适用性决策门禁 |
| 资源层 | AgentContributionTracker | collaboration | Agent贡献度追踪：权重/置信度记录、特征重要性输出、Top贡献者排名、集成模式分布统计 |
| 安全层 | ToolCallSecurityChain | security | 工具调用安全链：9层安全链(3层级)、Auto≤10ms快速路径、AST代码注入检测、Prompt注入检测(R47)、人工审批门 |

### 跨机器协作架构流程

```
任务提交 → DistributedTaskScheduler(资源感知调度+策略选择)
    → CrossMachineTransport(跨机器通信+消息路由)
        → coordinator角色 → 任务分配+状态同步
        → worker角色 → 任务执行+资源上报
        → peer角色 → 广播+直连通信
    → CollaborationModeRouter(协作模式路由)
    → AgentContributionTracker(贡献度追踪)
    → ToolCallSecurityChain(安全链校验)
    → 结果聚合 → 反馈闭环
```

## 工作区说明
- .harness/workspace/staging/ — 待审核代码暂存区
- .harness/workspace/shared/ — 已审核代码共享区
- .harness/workspace/locks/ — 编辑锁
- .harness/workspace/changelog.json — 变更日志

## .harness/rules/ 规则文件（16个）
- task-execution.md — 任务执行规则
- context-management.md — 上下文管理规则
- document-standards.md — 文档规范
- monitoring-fault-tolerance.md — 监控与容错规则
- cost-control.md — 成本控制规则
- security-permissions.md — 安全与权限规则
- best-practices.md — 最佳实践
- coding-standards.md — 编码规范
- ai-prompting-standards.md — AI提示词规范
- karpathy-principles.md — Karpathy原则
- memory-persistence.md — 记忆持久化规则
- anti-sycophancy.md — 反谄媚规则
- anti-bad-code.md — 反劣质代码规则
- token-efficiency.md — Token效率规则
- commit-discipline.md — Commit纪律规则
