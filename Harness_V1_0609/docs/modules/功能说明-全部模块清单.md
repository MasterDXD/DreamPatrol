﻿﻿﻿# 功能说明-全部模块清单

## 概述
本文档详细列出 Harness Engineering 多Agent框架 2.73.4 的全部模块，按功能层分类，明确各模块的核心功能、公共API、依赖关系和业务逻辑。

---

## 一、运行时引擎层（src/runtime/，100+个文件）

### 1.1 核心路由与编排

#### SkillRouter — 技能路由器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/skill/skill-router.js` |
| 继承 | `EventEmitter` |
| 功能 | 扫描`.harness/skills/`目录，解析YAML Frontmatter，根据用户消息和Agent角色自动匹配Skill，三层缓存架构（L1摘要/L2指令/L3资源），语义匹配，否定模式检测，内容去重 |
| 构造参数 | `projectRoot, options?: {cacheMax, cacheTTL, summaryMaxLength}` |
| 关键方法 | `discover()`, `match(context)`, `resolveConflict(matches)`, `checkDependencies(skillId, completedSkills)`, `loadL2(skillId)`, `loadL3(skillId)`, `smartPreload(context)`, `smartPreloadByPhase(phase)`, `getContextEstimate()`, `getDeduplicationReport()` |
| 事件 | `skills-reloaded` |
| 依赖 | constants, debug-logger |

#### MetaSkillOrchestrator — 元技能编排器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/skill/meta-skill-orchestrator.js` |
| 继承 | `EventEmitter` |
| 功能 | 将多个原子技能组合为单一可调用单元（Meta Skill），按多阶段流水线顺序执行，支持失败策略（stop/skip/retry）、模型路由决策（small/medium/large）、Token使用追踪、超时控制和阶段条件评估；内置5个预置Meta Skill（全栈构建/代码质量/调试修复/文档生成/调研构建）；支持通过LLM自动生成Meta Skill和从模板生成 |
| 构造参数 | `options?: {timeoutPerSkill, maxTotalTimeout, maxPhases, maxSkillsPerPhase, includePresets}` |
| 关键方法 | `registerMetaSkill(definition)`, `executeMetaSkill(metaSkillId, context)`, `getMetaSkill(metaSkillId)`, `listMetaSkills()`, `validateMetaSkill(definition)`, `generateMetaSkill(taskDescription, availableSkills)`, `generateMetaSkillAsync(taskDescription, availableSkills, llmHandler)`, `generateFromTemplate(templateName, params)`, `autoRegisterGenerated(definition)`, `cancelExecution(executionId)`, `getRunningExecutions()`, `estimateTokens(metaSkillId)`, `getStats()` |
| 事件 | `meta-skill-started`, `phase-started`, `skill-executed`, `phase-completed`, `meta-skill-completed`, `meta-skill-failed`, `model-routing-decision`, `token-usage`, `meta-skill-generated` |
| 依赖 | EventEmitter, safe-assign, debug-logger, safe-execute, constants, shutdown-mixin |

#### SessionManager — 会话管理器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/session/session-manager.js` |
| 继承 | `EventEmitter` |
| 功能 | 会话全生命周期管理（创建→运行→持久化→恢复），阶段转换验证，Token预算检查（80%/95%/100%阈值），防抖持久化（200ms），TTL过期（24h），最大100个会话 |
| 构造参数 | `projectRoot` |
| 关键方法 | `create(sessionId?)`, `get(sessionId)`, `advancePhase(sessionId, newPhase)`, `completeSkill(sessionId, skillId)`, `addTokenUsage(sessionId, tokens)`, `checkBudget(sessionId)`, `recordAgentAction(sessionId, agent, action)`, `recordDeepeningIteration(sessionId, data)`, `flush()`, `shutdown()` |
| 事件 | `session-created`, `phase-change`, `skill-complete`, `budget-warning`, `shutdown` |
| Bug修复 | TTL清理 `fs.promises.unlink` 失败现在记录错误而非静默吞没；`_evictOldest()` 现在跳过无效时间戳的会话而非选中它们进行驱逐；`advancePhase()` 和 `completeSkill()` 现在通过 `this.get(sessionId)` 返回防御性副本而非内部可变引用 |
| 依赖 | constants, errors, debounced-persister, debug-logger |

#### DocFreshnessGuard — 文档新鲜度守卫
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/quality/doc-freshness-guard.js` |
| 功能 | 检测过时文档，监控源码变更并标记关联文档为过期，提醒更新 |
| Bug修复 | `_processPendingChanges()` 现在检查 `_shutDown` 状态，防止关闭后继续执行；`_markStaleDocs()` 模块匹配改用 `module.startsWith(ref.replace(/\.\w+$/, '') + '/')` 替代 `module.startsWith(ref.replace(/\.\w+$/, ''))`，防止过度匹配（如 `utils/foo-bar` 不再匹配 `utils/foo`） |

#### PhaseOrchestrator — 阶段编排器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/phase-orchestrator.js` |
| 继承 | 无 |
| 功能 | 六阶段执行流程编排，阶段转换合法性验证，回退操作审批，阶段完成条件检查（strict Skill必须执行） |
| 构造参数 | 无 |
| 关键方法 | `getPhases()`, `canTransition(from, to)`, `validateRollback(from, to, completedSkills)`, `getRequiredSkills(phase)`, `isPhaseComplete(phase, completedSkills)`, `getNextPhase(currentPhase)` |
| Strict Skill集合 | `tdd-implement`, `module-development`, `code-review`, `verification-before-completion`, `bug-fix`, `security-audit`, `integration-testing`, `deployment`, `iterative-deepening` |
| 依赖 | constants |

#### CommandRouter — 命令路由器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/command-router.js` |
| 继承 | `EventEmitter` |
| 功能 | 扫描`.harness/commands/`目录，解析斜杠命令定义，支持ID/别名/斜杠前缀解析，模糊匹配（子序列匹配，阈值0.5），Tab补全，执行计划生成 |
| 构造参数 | `projectRoot` |
| 关键方法 | `discover()`, `resolve(input)`, `getExecutionPlan(commandId)`, `listCommands()`, `getHelpText(useColor?)`, `isCommand(input)`, `complete(partial)`, `fuzzyMatch(input)` |
| 事件 | `commands-discovered` |
| 依赖 | constants, debug-logger |

### 1.2 流程管控

#### PipelineExecutor — 管道执行器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/pipeline-executor.js` |
| 功能 | 核心请求处理管道，从用户消息到最终结果的完整执行流程：命令解析→意图识别→技能匹配→Hook前置检查→协作模式选择→任务构建→TDD门禁→执行（含超时控制）→证据验证→目标验证→覆盖率验证→Hook后置检查→性能日志 |
| 关键方法 | `executePipeline(ctx, userMessage, options)` |
| Bug修复 | AbortSignal 监听器现在在 finally 块中正确移除，防止内存泄漏（此前信号监听器未清理）；`structuredLog.logPerformance()` 调用前增加 null 检查，防止日志对象未初始化时 TypeError；超时 `setTimeout` 现在调用 `.unref()`，防止定时器阻止进程退出 |
| 依赖 | debug-logger, errors |

#### InstanceBuilder — 实例构建器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/instance-builder.js` |
| 功能 | 构建框架公开API实例，将内部上下文对象转换为用户友好的分层API（core/agents/gate/collaboration/store），管理实例生命周期（创建/销毁），优雅关闭序列（15s超时），全局错误处理器注册 |
| 关键方法 | `buildInstance(ctx, executePipeline)` |
| Bug修复 | 关闭超时定时器（`SHUTDOWN_TIMEOUT_MS`、`GRACEFUL_RESULT_WAIT_MS`）现在调用 `.unref()`，防止定时器阻止进程退出 |
| 依赖 | debug-logger, constants |

#### ModuleInitializer — 模块初始化器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/module-initializer.js` |
| 功能 | 框架核心模块的统一初始化与依赖注入，按序创建所有运行时/权限/门禁模块，记录启动耗时，失败时自动清理已创建模块 |
| 关键方法 | `_initCoreModules(projectRoot, validation, created)`, `_cleanup(created)` |
| Bug修复 | 所有创建的模块（commandRouter, programmableHookExecutor, contextCompressionEngine, thoughtModules, storeModules, agentModules, deepeningModules, collabModules, ragPipeline, autoVersionTracker）现在通过新增的 `_pushModules()` 辅助方法加入 `created` 清理数组，确保初始化失败时资源正确清理 |
| 依赖 | 所有核心模块构造函数 |

#### EventRegistrar — 事件注册器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/event-registrar.js` |
| 功能 | 集中管理框架内部事件转发和监听：Session→EventBus事件桥接、审计中间件注册、结构化日志监听器绑定 |
| 关键方法 | `_registerEventForwarding(ctx)`, `_registerSessionEvents(ctx)`, `_registerAuditMiddleware(ctx)`, `_registerStructuredLogListeners(ctx)` |

#### HealthRegistrar — 健康检查注册器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/health-registrar.js` |
| 功能 | 集中注册30+项健康检查和深化模块内部健康检查，覆盖核心模块、权限系统、门禁系统、深化管道等 |
| 关键方法 | `_registerHealthChecks(ctx)`, `_registerDeepeningInternalHealth(ctx)` |

#### ProgrammableHookExecutor — 可编程钩子执行器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/programmable-hook-executor.js` |
| 功能 | 支持builtin/function/shell三种Hook类型，10个内置处理器（path_validation, content_safety, tdd_check, evidence_check, design_anti_pattern_check, accessibility_compliance, design_token_validation, framework_compliance, karpathy_principles, memory_code_consistency），顺序执行+阻塞中断，慢Hook监控 |
| 关键方法 | `registerHook(event, hook)`, `executeHooks(event, context)`, `getHookStats()`, `getSlowHooks(threshold?)`, `getRegisteredHooks()` |
| Bug修复 | `getRegisteredHooks()` 未指定事件时现在一致返回空数组而非空对象 |

#### HookHandlers — 钩子处理器集合
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/hook-handlers.js` |
| 功能 | 10+内置Hook处理器实现，包括 `ai_service_check`（AI服务安全检查）、`security_check`（安全检查）、`design_anti_pattern_check`（设计反模式检测）、`accessibility_compliance`（无障碍合规）、`design_token_validation`（设计Token验证）、`framework_compliance`（框架合规）、`karpathy_principles`（Karpathy原则检查）、`memory_code_consistency`（记忆-代码一致性）等；内含 `RateLimitManager` 管理请求限流 |
| 关键方法 | 各处理器通过 `ProgrammableHookExecutor` 统一调度，`RateLimitManager.check()` 管理限流状态 |
| Bug修复 | `design_anti_pattern_check` 处理器现在内联 `_collectDesignViolations` 逻辑，而非通过 `this._collectDesignViolations()` 调用，修复处理器被提取后 `this` 上下文丢失导致的 TypeError；`RateLimitManager.check()` 现在检查 `_shutDown` 状态，防止关闭后继续累积限流数据导致内存增长 |
| 依赖 | ProgrammableHookExecutor |

#### GraphRAG — 文档图谱检索
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/graph-rag.js` |
| 继承 | `EventEmitter` |
| 功能 | 基于知识图谱的文档检索增强生成（GraphRAG），从文档内容中自动提取四类实体（PERSON/ORG/TECH/CONCEPT）和三类关系（DEPENDS_ON/PART_OF/RELATED_TO），段落级共现关系构建，社区聚类，图遍历查询，支持嵌入服务向量化 |
| 构造参数 | `options?: {maxEntities, maxRelations, maxClusters, cooccurrenceWindow, minRelationWeight}` |
| 关键方法 | `ingestDocument(docId, content, metadata?)`, `query(question, options?)`, `getEntity(name, type)`, `getRelations(entityId)`, `getCluster(clusterId)`, `buildClusters()`, `attachEmbeddingService(service)`, `getStats()` |
| 实体类型 | `PERSON`（人物）, `ORG`（组织）, `TECH`（技术）, `CONCEPT`（概念） |
| 关系类型 | `DEPENDS_ON`（依赖）, `PART_OF`（组成）, `RELATED_TO`（关联） |
| 事件 | `document-ingested`, `clusters-built`, `query-executed`, `error` |
| 依赖 | events, debug-logger, safe-execute, shutdown-mixin |

#### AgentSkillsDiscipline — 工程纪律插件
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/agent-skills-discipline.js` |
| 继承 | `EventEmitter` |
| 功能 | 七阶段工程纪律执行器（spec→plan→design→build→test→review→ship），强制阶段顺序执行与转换验证，每阶段输入上下文校验，回退规则定义，阶段完成追踪，斜杠命令映射 |
| 构造参数 | `config?: {strictOrder, allowRollback, requireCompletion}` |
| 关键方法 | `execute(command, context)`, `getCurrentStage()`, `getCompletedStages()`, `getStageResult(stage)`, `canTransitionTo(stage)`, `rollback(toStage)`, `getStats()` |
| 七阶段 | `spec`（规格）→ `plan`（规划）→ `design`（设计）→ `build`（构建）→ `test`（测试）→ `review`（审查）→ `ship`（发布） |
| 命令映射 | `spec→/spec`, `plan→/plan`, `design→/design`, `build→/build`, `test→/test`, `review→/review`, `ship→/ship` |
| 依赖 | events, debug-logger, shutdown-mixin |

#### ContextCompressionEngine — 上下文压缩引擎
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/context/context-compression-engine.js` |
| 功能 | 智能压缩策略，保留当前阶段Skill完整指令，压缩已完成Skill为摘要，支持策略配置，Token使用率80%时自动触发 |

#### TriggerDispatcher — 统一触发调度引擎
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/trigger-dispatcher.js` |
| 继承 | `EventEmitter` + `withShutdown` |
| 功能 | 统一触发调度引擎，管理五种触发模式：Cron表达式调度（简化版，支持*/、-五种语法）、间隔调度、Webhook路由（HMAC-SHA256签名验证）、事件订阅、即发即忘分发；支持ManagedAgentHost桥接，最大200个调度/100个Webhook路由/500个事件订阅 |
| 关键方法 | `registerSchedule(id, config)`, `unregisterSchedule(id)`, `registerWebhook(path, agentId)`, `registerEventSubscription(event, agentId)`, `dispatchTrigger(type, payload)`, `getStats()`, `attachManagedHost(host)`, `attachEventBus(bus)` |
| 事件 | `trigger-dispatched`, `trigger-failed`, `schedule-registered`, `schedule-unregistered`, `webhook-received` |
| 依赖 | events, unique-id, safe-execute, debug-logger, shutdown-mixin |

#### RalphWiggumLoop — 自主开发闭环
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/ralph-wiggum-loop.js` |
| 继承 | `EventEmitter` |
| 功能 | Harness Engineering 核心引擎，实现 OpenAI 提出的自主开发闭环（Ralph Wiggum Loop）：Prompt → Generate → Review → Test → Fix → Learn。采用深度优先策略将大任务拆解为小模块逐个完成，失败时回溯分析环境能力缺口而非简单重试。集成 MetaSkillOrchestrator（技能编排）、OptimizationLoop（迭代优化）、AdversarialReview（对抗审查）、CapabilityGapAnalyzer（能力缺口分析）、AutoReinLearningLoop（自增强学习）和 StateGraph（状态机编排） |
| 构造参数 | `options?: {maxIterations, maxDepth, reviewRounds, testTimeoutMs, convergeThreshold, autoFix, autoLearn, gapAnalysisOnFailure, haltOnCriticalGap, maxModuleCount}` |
| 关键方法 | `run(taskDescription, context)`, `cancel()`, `getState()`, `getProgress()`, `getCapabilityGaps()`, `getStats()`, `attachMetaSkillOrchestrator(o)`, `attachSkillExecutor(fn)`, `attachOptimizationLoop(loop)`, `attachAdversarialReview(review)`, `attachAutoReinLearningLoop(loop)`, `attachStateGraph(graph)`, `attachTestRunner(fn)` |
| 事件 | `state-change`, `module-start`, `module-complete`, `iteration-start`, `iteration-complete`, `gap-detected`, `review-result`, `test-result`, `loop-complete`, `loop-failed` |
| 依赖 | events, safe-assign, debug-logger, safe-execute, CapabilityGapAnalyzer |

#### CapabilityGapAnalyzer — 能力缺口分析器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/capability-gap-analyzer.js` |
| 继承 | `EventEmitter` |
| 功能 | Harness Engineering 核心组件，系统性诊断项目环境中的能力缺口，涵盖六维分析：技能（Skills）、工具（Tools）、规则（Rules）、CI/CD、文档（Docs）、测试（Tests）。当 AI Agent 执行任务失败时，不是简单地"再试一次"，而是回溯分析环境缺少了什么能力。支持能力注册表、缺口严重程度分级（critical/high/medium/low）、自动排序推荐和分析历史追踪 |
| 构造参数 | `options?: {maxRecommendations, severityThreshold, autoPrioritize, historySize}` |
| 关键方法 | `analyze(context)`, `registerCapability(dimension, name, metadata)`, `registerCapabilities(capabilities)`, `getGaps()`, `getRecommendations()`, `getRecommendationsByDimension(dimension)`, `getAnalysisHistory(limit)`, `getRegisteredCapabilities(dimension)`, `hasCapability(dimension, name)`, `getStats()` |
| 事件 | `gap-identified`, `analysis-complete`, `recommendation-added` |
| 依赖 | events, safe-assign, debug-logger, safe-execute, shutdown-mixin |

#### ProjectScaffolder — 项目脚手架生成器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/project-scaffolder.js` |
| 继承 | `EventEmitter` + `withShutdown` |
| 功能 | 项目脚手架自动生成器，融合Vibe Coding "PlanningWithFiles"核心能力，从需求描述自动生成项目文件结构；支持5种文件类型（entry/module/directory/config/test/docs/template/style），18种技术栈模板（来自templates-data.js），支持从模板或自然语言描述生成项目，桥接PhaseOrchestrator与Vibe Coding之间的能力缺口 |
| 关键方法 | `scaffold(templateName, options)`, `scaffoldFromDescription(description, options)`, `listTemplates()`, `getTemplate(templateName)`, `getStats()` |
| 事件 | `scaffold-completed`, `scaffold-failed` |
| 依赖 | fs, path, events, debug-logger, safe-execute, safe-assign, constants, fs-utils, shutdown-mixin |

#### BusinessGoal — 业务目标
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/business-goal.js` |
| 继承 | `EventEmitter` |
| 功能 | 业务目标绑定与KPI管理系统，支持KPI定义/更新/追踪，5维业务反思（business_impact/customer_satisfaction/cost_efficiency/compliance/feedback_quality），反馈来源管理，反馈历史记录 |
| 构造参数 | `config?: {maxFeedbackHistory}` |
| 关键方法 | `defineKpi(kpiId, name, target, unit)`, `updateKpi(kpiId, currentValue)`, `getKpi(kpiId)`, `reflectBusinessImpact(context)`, `getStats()` |
| 事件 | `kpi-defined`, `kpi-updated` |
| 依赖 | events |

#### RiskBasedApprovalGate — 风险审批门
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/risk-approval-gate.js` |
| 继承 | `EventEmitter` |
| 功能 | 基于风险等级的审批门控系统，4级风险分类（low/medium/high/critical），可配置自动批准阈值，操作风险规则定义，审批请求/批准/拒绝生命周期管理，持久化到`.harness/approvals/` |
| 构造参数 | `config?: {autoApproveThreshold}` |
| 关键方法 | `defineRiskLevel(operation, riskLevel, reason)`, `requiresApproval(action)`, `requestApproval(action, agentId)`, `approve(requestId, approver)`, `reject(requestId, rejecter)`, `getPending()`, `getPendingCount()`, `getHistory(limit)`, `getStats()` |
| 风险级别 | `low`（低风险）, `medium`（中风险）, `high`（高风险）, `critical`（关键风险） |
| 事件 | `risk-defined`, `risk-assessed`, `approval-requested`, `approval-approved`, `approval-rejected` |
| 依赖 | events, withShutdown |

#### HumanApprovalGate — 人工审批门
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/human-approval-gate.js` |
| 继承 | `EventEmitter` |
| 功能 | 关键决策点的人工确认机制，审批请求排队与超时管理，审批/拒绝/过期生命周期，持久化到`.harness/approvals/`，支持审批历史查询和统计 |
| 关键方法 | `requestApproval(action, agentId)`, `approve(requestId, approver)`, `reject(requestId, rejecter)`, `getPending()`, `getPendingCount()`, `getHistory(limit)`, `getStats()` |
| 事件 | `approval-requested`, `approval-approved`, `approval-rejected`, `approval-expired` |
| 依赖 | events, debounced-persister, debug-logger |

#### RagPipeline — RAG管道
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/rag-pipeline.js` |
| 继承 | `EventEmitter` |
| 功能 | 文档检索增强生成管道，从项目文档目录递归扫描Markdown文件，构建段落级索引，支持嵌入服务向量化检索，查询时按相关性排序返回文档片段，可集成GraphRAG叠加知识图谱实体结果，健康状态监控 |
| 构造参数 | `options?: {maxChunkSize, maxResults, chunkOverlap}` |
| 关键方法 | `initialize(docsRoot)`, `query(question, options?)`, `ingestDocument(docPath, content, metadata?)`, `ingestDirectory(dirPath, options?)`, `attachGraphRAG(graphRAG)`, `attachEmbeddingService(service)`, `getStats()` |
| 事件 | `pipeline-initialized`, `query-executed` |
| 依赖 | events, fs, path, debug-logger, safe-execute, shutdown-mixin |

### 1.3 协作编排

#### WorkflowDAG — 工作流有向无环图
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/workflow-dag.js` |
| 功能 | 工作流DAG管理，支持节点添加/删除、边连接/断开、拓扑排序、环检测，深化节点（DeepeningNode）作为特殊节点类型，可附加到父节点形成迭代深化子图 |
| 关键方法 | `addNode(id, data)`, `addEdge(from, to)`, `removeEdge(from, to)`, `addDeepeningNode(parentId, cfg)`, `topologicalSort()`, `hasCycle()` |
| Bug修复 | `addDeepeningNode()` 现在在移除边之前先检查是否会形成环，防止图状态不一致（此前先移除边再添加边，若添加边失败则图已处于不一致状态） |
| 依赖 | 无外部依赖 |

#### GoalExecutor — 目标执行器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/goal-executor.js` |
| 继承 | `EventEmitter` |
| 功能 | 长期目标执行框架，支持目标持久化（`.harness/goals/`），自动任务分解，自主迭代收敛循环，停滞检测（5轮改进低于阈值），TTL过期清理，并发控制（maxConcurrentGoals），异步原子写入 |
| 构造参数 | `options?: {maxIterations, convergenceThreshold, maxSubtasks, iterationTimeout, persistInterval, goalTTL, maxConcurrentGoals, autoDecompose, autoIterate, persistPath}` |
| 目标状态 | PENDING → DECOMPOSING → EXECUTING → ITERATING → PAUSED → COMPLETED / FAILED / CANCELLED / BLOCKED |
| 关键方法 | `createGoal(objective, options?)`, `execute(goalId, executeFn, options?)`, `pause(goalId)`, `resume(goalId)`, `cancel(goalId)`, `shutdown(signal)`, `getGoal(goalId)`, `getStats()`, `listGoals(statusFilter?)`, `getProgress(goalId)` |
| Attach方法 | `attachSessionManager()`, `attachPlanPersistence()`, `attachDeepeningOrchestrator()`, `attachSubagentExecutor()`, `attachThoughtRetrieverCycle()`, `attachCausalDataBus()` |

#### PlanExecuteOrchestrator — 规划-执行编排器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/plan-execute-orchestrator.js` |
| 继承 | `EventEmitter` + `withShutdown` |
| 功能 | 融合"AI先规划后执行"范式的核心编排器：规划→执行→验证→重新规划闭环，失败驱动三级升级策略（retry→replan→decompose），验证失败自动触发重新规划，步骤式执行+前置校验+后置验证 |
| 构造参数 | `config?: {maxStepRetries, maxReplanCount, maxDecomposeDepth, verifyFailureTriggersReplan, stepTimeoutMs, historySize}` |
| 计划状态 | PLANNING → EXECUTING → VERIFYING → REPLANNING → COMPLETED / FAILED |
| 步骤状态 | PENDING → RUNNING → COMPLETED / FAILED / SKIPPED / REPLANNED |
| 关键方法 | `execute(objective, fns, context?)`, `getStatus()`, `getStats()`, `getHistory(limit?)`, `shutdown()` |
| 回调函数 | `planFn(objective, context)`, `executeFn(step, context)`, `verifyFn?(step, result, context)`, `replanFn?(objective, failedStep, reason, context)`, `decomposeFn?(objective, context)` |
| 事件 | `goal-created`, `goal-completed`, `goal-failed`, `goal-paused`, `goal-resumed`, `goal-cancelled`, `goal-decomposing`, `goal-decomposed`, `goal-iteration-start`, `goal-iteration-complete` |
| Bug修复 | `shutdown()` 现在返回 `_shutdownWith()` 的 Promise，允许调用方 await 关闭完成（此前丢弃返回值导致无法等待关闭）；`_persistGoal()` 锁超时 `setTimeout` 现在调用 `.unref()`，防止定时器阻止进程退出 |
| 依赖 | constants, debug-logger |

#### CollaborationModeRouter — 协作模式路由器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/collaboration/collaboration-mode-router.js` |
| 继承 | `EventEmitter` |
| 功能 | 基于信号匹配+特征匹配+Agent数量自动选择协作模式，支持手动覆盖 |
| 五种模式 | `generator-verifier`（2-3 Agent）, `orchestrator-subagent`（3-5）, `agent-teams`（3-5）, `message-bus`（2-10）, `shared-state`（2-8） |
| 关键方法 | `selectMode(context)`, `overrideMode(sessionId, mode)`, `executeWithMode(task, executeFn, verifyFn)`, `getAllModes()` |
| 依赖 | events |

#### SubagentExecutor — 子智能体执行器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/subagent-executor.js` |
| 继承 | `EventEmitter` |
| 功能 | 子Agent生成与管理，并行执行，带验证的执行循环，模型解析（RBAC > ModelSelector > 默认），隔离上下文，超时控制（120s），结果截断（64KB），搜索结果缓存（TTL 5min） |
| 构造参数 | `options?: {maxConcurrent, defaultTimeout, maxSubagentsPerTask, tokenBudgetPerSubagent, enableResultStreaming, enableAutoRetry, maxRetries, defaultModel}` |
| 关键方法 | `spawn(task, agentConfig?)`, `spawnParallel(tasks, agentConfigs?)`, `executeParallel(tasks, configs, executeFn)`, `executeWithVerification(task, config, executeFn, verifyFn)`, `cancel(handleId)`, `cancelAll()` |
| 事件 | `subagent-spawned`, `subagent-started`, `subagent-completed`, `subagent-failed`, `subagent-cancelled`, `subagent-retry`, `spawn-rejected` |
| 依赖 | constants, debug-logger, isolated-context-manager |

#### PairChat — 结对编程
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/collaboration/pair-chat.js` |
| 继承 | `EventEmitter` |
| 功能 | 两个Agent结对编程，实时代码共享，共识机制，支持提议-审查-批准/拒绝流程，可配置自动批准阈值 |
| 角色常量 | `CHAT_ROLES`: PROPOSER, REVIEWER |
| 共识状态 | `CONSENSUS_STATES`: PENDING, APPROVED, REJECTED, NEEDS_REVISION |
| 配置 | `maxRounds`（默认10）, `requireApproval`（默认true）, `autoApproveThreshold`（默认0.9） |
| 关键方法 | `startSession(config)`, `propose(sessionId, content)`, `review(sessionId, corrections)`, `approve(sessionId)`, `reject(sessionId, reason)`, `getSession(sessionId)`, `getStats()` |
| 事件 | `session-started`, `proposal-made`, `review-completed`, `consensus-reached`, `consensus-rejected` |
| Bug修复 | `_evaluateConsensus()` 现在尊重 `requireApproval` 配置——当 `requireApproval=true` 且未显式批准时返回 PENDING；`_onShutdown()` 统计结构现在与初始化结构一致 `{ totalCorrections, totalRounds, totalSessions }` |
| 依赖 | events |

#### ChatChain — 链式对话
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/collaboration/chat-chain.js` |
| 继承 | `EventEmitter` |
| 功能 | 多Agent链式对话，顺序传递上下文，支持任务依赖阻塞检测，预定义阶段链模板，自动重试机制 |
| 任务状态 | `TASK_STATUS`: PENDING, IN_PROGRESS, COMPLETED, SKIPPED, FAILED, BLOCKED |
| 预定义链 | `ATOMIC_TASK_CHAINS`: brainstorming, requirement-analysis, architecture-design 阶段模板 |
| 关键方法 | `createChain(phase, customTasks)`, `startTask(chainId, taskId)`, `completeTask(chainId, taskId, output)`, `failTask(chainId, taskId, error)`, `retryTask(chainId, taskId)`, `getChain(chainId)`, `getStats()` |
| 事件 | `chain-created`, `task-started`, `task-completed`, `task-failed`, `chain-completed`, `chain-failed` |
| Bug修复 | `startTask()` 现在将 `this._buildTaskIndex(chain)` 传递给 `_isTaskBlocked()` 以防止 TypeError；`failTask()` 将 `task._retryCount <= MAX_RETRIES` 改为 `task._retryCount < MAX_RETRIES` 修正重试逻辑；`_onShutdown()` 统计结构现在与初始化结构一致 `{ totalChains, completedChains, failedChains, totalTasks, completedTasks }`；链淘汰时同步删除`_annotations`中对应条目防止孤立数据泄漏；`_onShutdown()`添加`removeAllListeners()` |
| 依赖 | events |

#### OutputFusion — 输出融合
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/collaboration/output-fusion.js` |
| 继承 | `EventEmitter` |
| 功能 | 多Agent输出结果融合策略，支持多数投票/加权平均/最优选择/共识四种内置策略，可注册自定义策略 |
| 内置策略 | `majority-vote`（多数投票）, `weighted-average`（加权平均）, `best-of`（最优选择）, `consensus`（共识） |
| 策略配置 | `minAgreement`（默认0.6）, `weightKey`（默认'confidence'） |
| 关键方法 | `fuse(outputs, strategy)`, `registerStrategy(name, strategyFn)`, `getStrategies()`, `getStats()` |
| 事件 | `fusion-complete`, `strategy-registered` |
| Bug修复 | `_extractVote()` 现在在 `String()` 转换前检查 `undefined`/`null`，防止 "undefined"/"null" 字符串被当作有效投票；`_weightedFusion` 非数值字段按权重降序排列后取首个，确保高权重Agent输出优先 |
| 依赖 | events |

#### EnsembleOrchestrator — 集成编排器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/collaboration/ensemble-orchestrator.js` |
| 继承 | `EventEmitter` + `withShutdown` |
| 功能 | 三种集成模式编排：Bagging（并行求稳）、Boosting（串行纠错）、Stacking（元学习），Agent执行超时保护，历史记录管理 |
| 构造参数 | `options?: {mode, maxBoostingRounds, maxAgents, agentTimeoutMs, maxHistory}` |
| 关键方法 | `execute(task, agents, options?)`, `setMode(mode)`, `getHistory()`, `getModes()` |
| 事件 | `ensemble:start`, `ensemble:complete`, `ensemble:agent-error` |
| Bug修复 | Agent执行添加60s超时保护（`_withTimeout`）；Boosting模式传递`{previousResults, round}`上下文给后续Agent；添加`withShutdown`混入和`_onShutdown`清理；Agent失败时发射`ensemble:agent-error`事件 |
| 依赖 | events, shutdown-mixin |

#### AgentDiversityManager — Agent多样性管理器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/collaboration/agent-diversity-manager.js` |
| 继承 | `EventEmitter` |
| 功能 | 四维多样性评估（角色/方法/错误/视角），集成学习模式推荐（Bagging/Boosting），Agent注册与profile管理 |
| 构造参数 | `options?: {maxProfiles}` |
| 关键方法 | `registerAgent(agentId, profile)`, `getDiversityScore()`, `getEnsembleRecommendation(task)`, `recordError(agentId, error)`, `recordSuccess(agentId, result)` |
| 事件 | `agent-registered`, `diversity-assessed` |
| Bug修复 | `_computeErrorDiversity`中无错误Agent对跳过比较（`continue`），不再误增overlap导致多样性分数偏低 |
| 依赖 | events |

### 1.4 深化推理层

#### DeepeningOrchestrator — 深化编排器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/deepening/deepening-orchestrator.js` |
| 继承 | `DeepeningBase` |
| 功能 | 深化推理核心编排，深度级别映射（quick→1轮, standard→1轮, deep→2轮, intensive→4轮），并行Agent执行，收敛检测+生成器验证双重保障，策略插件决策，结果融合，思维检索闭环 |
| 构造参数 | `options?: {defaultDepthLevel, maxIterations, convergenceThreshold, topK, fusionStrategy, tokenBudgetRatio, enableLTI, enableAdaptiveDepth, enableMultiAgent, enableMetrics, enableCache, enableStrategyPlugin, enableReportGeneration, parallelAgentExecution, maxConcurrentExecutions}` |
| 关键方法 | `execute(task, agents, options?)`, `getExecutionLog(limit?)`, `generateReport(type, data)`, `getStats()` |
| Attach方法 | `attachStrategyProvider(sp)`, `attachConvergenceDetector(cd)`, `attachCache(c)`, `attachMetricsCollector(mc)`, `attachQualityScorer(qs)`, `attachEmbeddingService(es)`, `attachThoughtRetrieverCycle(trc)`, `attachReportGenerator(rg)`, `attachEventStore()`, `attachGeneratorVerifier()`, `attachSubagentExecutor()` |
| 事件 | `execution-start`, `execution-complete`, `depth-assessed`, `agents-selected`, `iteration-complete`, `iteration-verified`, `converged`, `strategy-stop`, `agent-error`, `cache-hit` |
| Bug修复 | `execute()` 入口新增 `_shutDown` 状态检查，关闭后拒绝执行；`_shouldStopIterating()` 修正 `ConvergenceDetector.check()` 调用为 `check(task.id \|\| 'default', { qualityScore: lastScore })`，正确传递 task 参数；`_runThoughtCycle()` 改为 `async` 并正确 `await` `trc.execute()`（此前未 await 导致思维检索闭环异步竞态）；`_execLog` 存储 `taskId` 替代完整 `task` 对象引用，防止内存泄漏；`Math.max(...qualityHistory)` 替换为 `reduce()` 实现，防止大数组栈溢出 |
| 依赖 | constants, debug-logger |

#### ThoughtRetrieverCycle — 思维检索循环
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/thought/thought-retriever-cycle.js` |
| 继承 | `EventEmitter` |
| 功能 | 五步循环：检索思想→生成回答→提炼思想与置信度→查重去冗余→更新记忆，三种检索模式（confidence/semantic/hybrid） |
| 构造参数 | `options?: {thoughtExtractor, thoughtDeduplicator, thoughtMemoryStore, embeddingService, retrievalMode, semanticWeight, confidenceWeight}` |
| 关键方法 | `execute(agentOutput, context?)`, `retrieve(context?)`, `getStats()` |
| 事件 | `cycle-complete`, `step-complete`, `step-error` |
| Bug修复 | `_onShutdown()` 不再关闭共享依赖（thoughtMemoryStore, embeddingService），仅清除自身引用 |
| 依赖 | events |

#### DeepeningPipeline — 深化管道
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/deepening/deepening-pipeline.js` |
| 继承 | `DeepeningBase` |
| 功能 | 深化推理管道，串联21个子模块（调度器/控制器/路由器/融合/缓存/熔断/限流等），统一初始化与关闭生命周期管理，按序编排子模块执行 |
| 关键方法 | `initialize()`, `run(task, agents)`, `getStats()` |
| 事件 | `pipeline-initialized`（初始化完成）, `pipeline-run-start`, `pipeline-run-complete`, `pipeline-error` |
| Bug修复 | `_onShutdown()` 现在按序调用所有子模块的 `shutdown()` 后再清除引用（此前直接清除引用导致子模块资源未释放）；`run()` 入口新增 `_shutDown` 状态检查；`agents.length` 替换为安全计算 `Array.isArray(agents) ? agents.length : Object.keys(agents).length`，兼容数组和对象两种Agent格式；`initialize()` 事件名从 `pipeline-start` 改为 `pipeline-initialized`，避免与 `run()` 的启动事件混淆 |
| 依赖 | DeepeningBase, DeepeningModuleRegistry |

#### ProgressiveDeepening — 渐进深化
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/deepening/progressive-deepening.js` |
| 继承 | `DeepeningBase` |
| 功能 | 从浅到深逐步推理，四级深度配置（quick/standard/deep/intensive），每级对应不同迭代次数，支持对抗审查和可配置早停阈值 |
| 关键方法 | `execute(agent, task, level, options?)`, `getLevelConfig(level)`, `getStats()` |
| 深度级别 | `quick`（1次迭代）, `standard`（2次迭代）, `deep`（3次迭代）, `intensive`（4次迭代） |
| 配置 | `_earlyStopThreshold`（可配置早停质量阈值，替代原硬编码0.95） |
| Bug修复 | `execute()` 新增 `result` 存在性检查，仅在 `result` 非空时传递给对抗审查器（此前 `result` 为 `undefined` 时导致审查器异常）；硬编码质量阈值 0.95 替换为可配置的 `_earlyStopThreshold`；新增 `_onShutdown()` 重置 `_levelCounts` 统计数据 |
| 依赖 | DeepeningBase |

#### TokenAwareDeepening — Token感知深化
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/deepening/token-aware-deepening.js` |
| 继承 | `DeepeningBase` |
| 功能 | 在Token预算约束下执行深化推理，根据剩余预算动态计算最大迭代次数，记录每次迭代的Token消耗和质量分数，支持迭代成本-收益分析 |
| 关键方法 | `calculateMaxIterations(tokenManager, sessionId)`, `canAffordIteration(tokenManager, sessionId, tokenCost)`, `recordIterationCost(sessionId, iteration, tokensUsed, qualityScore)` |
| 配置 | `maxIterationCap`（默认10，迭代次数上限，防止资源耗尽） |
| Bug修复 | `calculateMaxIterations()` 新增 `maxIterationCap \|\| 10` 上限约束，防止Token预算充足时迭代次数无限增长；`recordIterationCost()` 新增输入类型验证（`tokensUsed` 和 `qualityScore` 必须为数字），非法输入时跳过计算避免NaN传播 |
| 依赖 | DeepeningBase |

#### OodaLoop — OODA决策闭环
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/deepening/ooda-loop.js` |
| 继承 | `DeepeningBase` |
| 功能 | OODA（观察-判断-决策-行动）决策闭环引擎，支持三种决策模式（reactive/deliberate/creative），信号威胁/机会级别计算，决策偏见检测，自动循环执行 |
| 构造参数 | `options?: {maxHistorySize, autoLoop, threatThreshold, opportunityThreshold, observationWindow}` |
| 关键方法 | `observe(taskContext, agentState, environmentSignals)`, `orient(observation)`, `decide(orientation)`, `act(decision, context)`, `execute(context)`, `reset()`, `getStats()` |
| 决策模式 | `REACTIVE`（威胁响应）, `DELIBERATE`（审慎分析）, `CREATIVE`（机会探索） |
| 事件 | `ooda-observed`, `ooda-oriented`, `ooda-decided`, `ooda-acted`, `ooda-cycle-completed`, `ooda-auto-loop-triggered`, `ooda-reset` |
| 依赖 | DeepeningBase, debug-logger |

#### DeepeningErrorHandler — 深化错误处理器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/deepening/deepening-error-handler.js` |
| 继承 | `DeepeningBase` |
| 功能 | 深化管道的错误分类、恢复和重试管理，支持错误类型路由、自动恢复策略、错误事件发射 |
| 关键方法 | 错误分类与路由、恢复策略执行、错误事件发射 |
| 依赖 | DeepeningBase |

#### DeepeningConfigManager — 深化配置管理器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/deepening/deepening-config-manager.js` |
| 继承 | `DeepeningBase` |
| 功能 | 深化管道的配置加载、验证和合并，支持环境覆盖、Schema验证、热重载 |
| 关键方法 | 配置加载与合并、Schema验证、热重载 |
| 依赖 | DeepeningBase |

#### DeepeningNotifier — 深化通知器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/deepening/deepening-notifier.js` |
| 继承 | `DeepeningBase` |
| 功能 | 深化管道的进度通知和状态变更广播，支持事件订阅、通知分级、异步分发 |
| 关键方法 | 进度通知、状态变更广播、事件订阅管理 |
| 依赖 | DeepeningBase |

#### DeepeningValidator — 深化验证器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/deepening/deepening-validator.js` |
| 继承 | `DeepeningBase` |
| 功能 | 深化管道的输出验证和质量门禁，支持多维度验证规则、阈值检查、验证报告 |
| 关键方法 | 多维度验证、阈值检查、验证报告生成 |
| 依赖 | DeepeningBase |

#### 深化子模块（53个，通过DeepeningModuleRegistry懒加载）

所有深化子模块均继承自 `DeepeningBase`（`src/runtime/deepening/deepening-base.js`），该基类提供统一的 `isHealthy()`、`getStats()`、`shutdown()` 和 27个 `attach*()` 方法，消除了各模块间的重复代码。

| 模块 | 文件 | 功能 |
|------|------|------|
| **DeepeningBase** | `deepening-base.js` | 深化模块公共基类，提供 shutdown/isHealthy/getStats/attach* 统一接口 |
| DeepeningErrorHandler | `deepening-error-handler.js` | 错误处理；基于消息模式（timeout/convergence/invalid/resource）和上下文类型自动分类错误，错误历史记录与统计 |
| DeepeningCache | `deepening-cache.js` | 深化结果缓存；基于任务键的缓存存储/检索/失效，质量分数感知更新（仅缓存更高质量结果），WeakMap键缓存优化序列化，BoundedMap容量限制与淘汰统计 |
| DeepeningCircuitBreaker | `deepening-circuit-breaker.js` | 熔断器（连续失败时断开） |
| DeepeningRateLimiter | `deepening-rate-limiter.js` | 限流器（令牌桶算法） |
| DeepeningEventBus | `deepening-event-bus.js` | 深化子系统事件总线 |
| DeepeningConnectionPool | `deepening-connection-pool.js` | 连接池管理 |
| DeepeningTaskQueue | `deepening-task-queue.js` | 任务队列 |
| DeepeningServiceRegistry | `deepening-service-registry.js` | 服务注册表 |
| DeepeningResourceManager | `deepening-resource-manager.js` | 资源管理；Bug修复：`getResourceInfo()` 现在返回 `Object.assign({}, info)` 替代直接内部引用，防止外部修改污染内部状态；`acquire()` 超时定时器现在调用 `.unref()`，防止定时器阻止进程退出 |
| DeepeningPriorityQueue | `deepening-priority-queue.js` | 优先级队列 |
| DeepeningNotifier | `deepening-notifier.js` | 通知器 |
| DeepeningLoadBalancer | `deepening-load-balancer.js` | 负载均衡 |
| DeepeningHealthMonitor | `deepening-health-monitor.js` | 健康监控 |
| DeepeningErrorHandler | `deepening-error-handler.js` | 错误处理 |
| DeepeningDataPipeline | `deepening-data-pipeline.js` | 数据管道 |
| DeepeningTimeoutManager | `deepening-timeout-manager.js` | 超时管理 |
| DeepeningStateManager | `deepening-state-manager.js` | 状态管理 |
| DeepeningSnapshot | `deepening-snapshot.js` | 快照 |
| DeepeningSnapshotStore | `deepening-snapshot-store.js` | 快照存储 |
| DeepeningSecurityGuard | `deepening-security-guard.js` | 安全守卫 |
| DeepeningRetryPolicy | `deepening-retry-policy.js` | 重试策略 |
| DeepeningReportGenerator | `deepening-report-generator.js` | 报告生成；Bug修复：`_qualityTrend()` 将 `Math.max(...scores)` 替换为 `reduce()` 模式，防止大数组展开导致 RangeError 栈溢出 |
| DeepeningPluginSystem | `deepening-plugin-system.js` | 插件系统 |
| DeepeningMetricsCollector | `deepening-metrics-collector.js` | 指标收集；Bug修复：`getTimeSeries()` 现在返回数据点的防御性副本（含 `Object.assign({}, p.tags)`），而非内部数组引用，防止外部修改污染内部数据 |
| DeepeningMetricsAggregator | `deepening-metrics-aggregator.js` | 指标聚合 |
| DeepeningLockManager | `deepening-lock-manager.js` | 锁管理；Bug修复：`getLocksByOwner()` 和 `getExpiredLocks()` 现在返回防御性副本（含安全字段），而非内部锁对象引用，防止外部修改污染内部状态 |
| DeepeningGracefulShutdown | `deepening-graceful-shutdown.js` | 优雅关闭 |
| DeepeningDeployment | `deepening-deployment.js` | 部署管理 |
| DeepeningDependencyResolver | `deepening-dependency-resolver.js` | 依赖解析 |
| DeepeningBenchmark | `deepening-benchmark.js` | 基准测试 |
| DeepeningBackpressureManager | `deepening-backpressure-manager.js` | 背压管理 |
| DeepeningWorkflowTemplate | `deepening-workflow-template.js` | 工作流模板 |
| DeepeningTaskScheduler | `deepening-task-scheduler.js` | 任务调度；Bug修复：`getTask()` 现在返回防御性副本替代内部任务引用；`getPending()` 和 `getRunning()` 返回含安全字段的副本；定时器ID现在使用 `unref()` 不阻止进程退出 |
| DeepeningEventReplay | `deepening-event-replay.js` | 事件回放 |
| DeepeningAuditTrail | `deepening-audit-trail.js` | 审计追踪 |
| DeepeningConfigManager | `deepening-config-manager.js` | 配置管理 |
| DeepeningModuleRegistry | `deepening-module-registry.js` | 模块注册表（懒加载入口） |
| DeepeningStateMachine | `deepening-state-machine.js` | 状态机；Bug修复：`createExecution()` 现在返回安全副本 `{ executionId, currentState }` 替代完整内部 exec 对象，防止外部修改 `currentState` 污染内部状态 |
| DeepeningStrategyPlugin | `deepening-strategy-plugin.js` | 策略插件 |
| DeepeningThrottle | `deepening-throttle.js` | 节流器 |
| DeepeningEventStore | `deepening-event-store.js` | 事件存储；Bug修复：`_loadFromDisk()` 和 `_loadFromDiskAsync()` 现在在从磁盘加载事件后重建 `_byType` 和 `_byExecution` 索引，修复重启后索引过期/不完整的问题 |
| DeepeningVisualizer | `deepening-visualizer.js` | 可视化 |
| DeepeningFeatureFlags | `deepening-feature-flags.js` | 特性开关 |
| DeepeningValidator | `deepening-validator.js` | 验证器 |

### 1.5 思维与记忆

#### ThoughtExtractor — 思维提取器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/thought/thought-extractor.js` |
| 功能 | 从Agent输出中提取结构化思维，包含置信度评估 |

#### ThoughtDeduplicator — 思维去重器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/thought/thought-deduplicator.js` |
| 功能 | 检测并合并重复思维，支持语义相似度比较 |

#### ThoughtMemoryStore — 思维记忆存储
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/thought/thought-memory-store.js` |
| 功能 | 思维持久化存储，支持按置信度和语义检索 |
| Bug修复 | `storeThought()` 现在处理同步和异步 `embed()` 返回值，防止 Promise 对象被存储为向量 |

#### MemoryNudge — 记忆推动
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/thought/memory-nudge.js` |
| 功能 | 主动提醒相关历史决策，基于事件监听的记忆推送 |
| Bug修复 | `startListening()` 新增重复注册防护；`_consolidateMemories()` 现在先添加合并条目再移除旧条目，防止失败时数据丢失 |

#### DreamEngine — 跨会话学习引擎
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/thought/dream-engine.js` |
| 继承 | `EventEmitter` |
| 功能 | 跨会话学习与知识蒸馏引擎，模拟"做梦"过程从历史会话中提取错误规避/最佳实践/工作流优化三类模式笔记，模式频率分析与置信度评估，笔记去重合并，支持SQLite持久化和嵌入服务 |
| 构造参数 | `options?: {maxNotes, maxSessionsPerDream, similarityThreshold, minPatternFrequency, minConfidence}` |
| 关键方法 | `startDreaming(sessionHistory)`, `getRelevantNotes(context)`, `getNotes(category?, minConfidence?)`, `getStats()`, `attachSqliteStore(store)`, `attachThoughtMemoryStore(store)`, `attachEmbeddingService(service)` |
| 笔记类别 | `error-avoidance`（错误规避）, `best-practice`（最佳实践）, `workflow-optimization`（工作流优化） |
| 事件 | `dream-complete`, `dream-error` |
| 依赖 | events, constants, debug-logger, safe-execute, shutdown-mixin |

#### BrainMemory — Brain记忆系统
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/thought/brain-memory.js` |
| 继承 | `EventEmitter` |
| 功能 | 分层记忆系统，支持关键词/语义/混合三种检索模式，TF-IDF关键词匹配+嵌入向量语义相似度融合排序，记忆TTL过期与LRU淘汰，定期整合（consolidation），健康状态监控（healthy/degraded/unhealthy），SQLite持久化 |
| 构造参数 | `options?: {maxMemories, defaultTTL, retrievalTimeout, consolidationInterval, similarityThreshold}` |
| 关键方法 | `store(key, content, metadata?)`, `retrieve(query, options?)`, `getHealthStatus()`, `getStatusBar()`, `attachSqliteStore(store)`, `attachEmbeddingService(service)`, `attachEventBus(bus)` |
| 检索模式 | `keyword`（关键词）, `semantic`（语义）, `hybrid`（混合，默认） |
| 事件 | `memory-stored`, `memory-retrieved` |
| 依赖 | events, debug-logger, safe-assign, safe-execute, shutdown-mixin |

#### LLMWiki — LLM知识库
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/thought/llm-wiki.js` |
| 继承 | `EventEmitter` |
| 功能 | LLM知识库管理系统，基于Markdown文件的分类知识存储（concepts/decisions/patterns/api/troubleshooting），双向链接索引与反向链接追踪，Frontmatter元数据管理，自动索引加载，支持SQLite持久化和嵌入服务 |
| 构造参数 | `options?: {categories, maxEntriesPerCategory, autoIndex}` |
| 关键方法 | `initialize(wikiRoot)`, `createEntry(category, title, content, metadata?)`, `getEntry(category, slug)`, `search(query, options?)`, `getBacklinks(category, slug)`, `listEntries(category?)`, `attachSqliteStore(store)`, `attachEmbeddingService(service)` |
| 知识类别 | `concepts`（概念）, `decisions`（决策）, `patterns`（模式）, `api`（接口）, `troubleshooting`（排错） |
| 事件 | `initialized`, `entry-created`, `entry-updated`, `entry-deleted` |
| 依赖 | events, fs, path, debug-logger, safe-execute, fs-utils, constants, shutdown-mixin |

#### EmbeddingService — 嵌入服务
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/model/embedding-service.js` |
| 功能 | 文本向量化嵌入服务，用于语义检索 |
| Bug修复 | 移除手动缓存驱逐逻辑（`_cache.size >= cacheMaxSize` 检查及 `_cache.delete(firstKey)`），该逻辑与 LRUCache 内部驱逐机制冲突 |

#### ModelSelector — 模型选择器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/model/model-selector.js` |
| 功能 | 模型无关性设计，分层抽象（premium/standard/economy），策略映射表，降级链，使用量追踪 |

#### TriAttention — TriAttention上下文优化
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/model/tri-attention.js` |
| 继承 | `EventEmitter` |
| 功能 | Pre-RoPE空间三角级数评分+向量幅度双引擎注意力计算器，综合三角级数距离偏好、向量幅度、自适应权重调整三维度估算注意力权重，支持离线校准Q/K聚类中心，用于上下文优化和KV缓存压缩，按注意力阈值和Token预算裁剪上下文条目，保留高注意力条目、裁剪低注意力条目，输出压缩比和Token节省量 |
| 构造参数 | `options?: {attentionThreshold, maxContextEntries, recencyDecay}` |
| 关键方法 | `optimize(contextWindow, budget)`, `estimateAttention(entry, position, total)`, `getStats()` |
| 配置 | `attentionThreshold`（默认0.3，注意力阈值）, `maxContextEntries`（默认500）, `recencyDecay`（默认0.95，时效衰减系数） |
| 事件 | `optimized` |
| 依赖 | events, errors, safe-assign, debug-logger, shutdown-mixin |

#### KVCacheManager — KV缓存管理器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/model/kv-cache-manager.js` |
| 继承 | `EventEmitter` |
| 功能 | 基于TriAttention校准的KV缓存压缩器，三角级数距离偏好+向量幅度+时效性+访问频率四维评分剪枝，自适应权重调整，10倍+显存压缩 |
| 构造参数 | `options?: {maxCacheSize, compressionRatio, enableAdaptiveWeights}` |
| 关键方法 | `compress(kvCache, options)`, `scoreEntry(entry, context)`, `calibrate(triAttention)`, `getStats()` |
| 事件 | `compressed` |
| 依赖 | events, errors, safe-assign, debug-logger, shutdown-mixin |

#### CostAwareRouter — 成本感知技能路由器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/model/cost-aware-router.js` |
| 继承 | `EventEmitter` |
| 功能 | 基于五维复杂度信号（任务类型/依赖数/约束数/上下文长度/历史耗时）分析将请求路由到最经济的模型层级（small/medium/large），追踪每技能 Token 消耗与累计成本，支持上下文缓存感知（自动识别可缓存请求并记录缓存命中），Token 预算预警（80%/95%/100% 三级阈值），成本汇总与统计报告 |
| 构造参数 | `options?: {defaultModelTier, costThresholds, enableCacheAwareness, budgetWarningThresholds, maxUsageHistory}` |
| 关键方法 | `analyzeComplexity(task)`, `recordUsage(skillId, tokens, modelTier)`, `getSkillCosts(skillId)`, `getCostSummary()`, `getStats()`, `isCacheable(context)`, `recordCacheHit(skillId)` |
| 模型层级 | `small`（经济型）, `medium`（标准型）, `large`（高性能型） |
| 依赖 | EventEmitter, shutdown-mixin, safe-assign, debug-logger |

### 1.6 Agent管理

#### AgentRuntime — Agent运行时
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/agent-runtime.js` |
| 功能 | Agent注册、资源分配、运行时管理 |

#### AgentLifecycleController — Agent生命周期控制器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/agent-lifecycle-controller.js` |
| 功能 | Agent创建→启动→停止→销毁全生命周期管理 |

#### AgentSandbox — Agent沙箱
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/agent-sandbox.js` |
| 功能 | Agent执行沙箱隔离，防止资源越界 |
| Bug修复 | `prepare()` 重构提取 `_evictIfNeeded()` 和 `_findEvictionCandidate()` 辅助方法，降低圈复杂度（22→20以下）；STRICT级别 `maxExecutionTimeMs` 改用专用常量 `DEFAULT_SANDBOX_STRICT_TIMEOUT_MS`（30s），不再误用缓存TTL常量 `DEFAULT_TTL_CACHE_MS`；移除未使用的 `DEFAULT_TTL_CACHE_MS` 导入 |

#### AgentMonitor — Agent监控器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/agent-monitor.js` |
| 功能 | Agent运行监控，反模式检测，告警机制 |

#### AgentStateManager — Agent状态管理器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/agent-state-manager.js` |
| 功能 | Agent状态快照、恢复、持久化 |

#### AgentDeployment — Agent部署管理
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/agent-deployment.js` |
| 功能 | Agent部署环境管理、部署流程编排 |
| Bug修复 | `_persistDeployment()` 现在在部署成功和部署失败时均被调用，确保部署记录立即持久化到磁盘，而非仅在显式 `flush()` 时写入 |

#### AgentWorkflowIntegration — Agent工作流集成
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/agent-workflow-integration.js` |
| 功能 | Agent与工作流DAG的集成，任务依赖管理 |
| Bug修复 | 异步任务新增超时保护：通过 `setTimeout` 配合 `settled` 守卫标志防止双重反馈提交；超时使用任务记录中的 `task.timeout`；定时器调用 `.unref()` 不阻止进程退出 |

#### AgentChannel — Agent通道
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/agent-channel.js` |
| 功能 | Agent间通信通道，消息路由 |

#### SkillWrapper — 视觉推理衔接
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/skill-wrapper.js` |
| 继承 | `EventEmitter` |
| 功能 | 视觉输入到技能推理的衔接器，从视觉元素/空间关系/描述文本中提取五类谓词（spatial/attribute/relational/temporal/existential），将谓词桥接映射为技能输入和推理链，支持最大桥接深度限制和谓词缓存 |
| 构造参数 | `options?: {maxPredicates, minConfidence, maxBridgeDepth}` |
| 关键方法 | `inventPredicate(visualInput, taskContext)`, `bridgeToReasoning(predicates, taskGoal)`, `getStats()` |
| 谓词类型 | `SPATIAL`（空间）, `ATTRIBUTE`（属性）, `RELATIONAL`（关系）, `TEMPORAL`（时序）, `EXISTENTIAL`（存在） |
| 事件 | `predicates-invented`, `bridge-created` |
| 依赖 | events, errors, safe-assign, debug-logger, shutdown-mixin |

#### AgentPackManager — Agent包管理器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/agent-pack-manager.js` |
| 功能 | Agent包的安装、卸载、版本管理 |

#### AgentDebugLoop — Agent自调试闭环
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/agent-debug-loop.js` |
| 功能 | 自主运行测试→分析失败→修复代码→重新测试的完整自动化循环，支持最大迭代次数限制和收敛检测，可集成DeepeningOrchestrator在多次迭代失败后深化分析 |
| 关键方法 | `execute(task)`, `reset()`, `getStats()`, `attachDeepeningOrchestrator(orchestrator)` |
| 事件 | `loop:start`, `loop:complete`, `loop:failed`, `iteration:start`, `iteration:end`, `deepening:applied`, `deepening:failed` |

#### HarnessLayer — Harness层抽象
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/harness-layer.js` |
| 功能 | Agent与Harness框架交互的抽象层，提供统一的能力注册和事件订阅接口 |

#### ModelLayer — 模型层抽象
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/agent/model-layer.js` |
| 功能 | Agent与LLM模型交互的抽象层，支持多模型切换和降级策略 |

### 1.7 技能管理

#### SkillGraph — 技能图谱
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/skill/skill-graph.js` |
| 继承 | `EventEmitter` |
| 功能 | 技能关系图谱，节点表示技能、边表示依赖/阻塞/语义/因果四类关系，支持从Skill定义批量构建图谱，语义分组自动关联，拓扑排序与环检测，影响范围分析（受影响节点/依赖链），可达性查询 |
| 构造参数 | `config?: {maxNodes, maxEdges, semanticMatchThreshold}` |
| 关键方法 | `addNode(skillId, metadata)`, `addEdge(fromId, toId, edgeType, weight?)`, `buildFromSkills(skills)`, `topologicalSort()`, `hasCycle()`, `getDependents(skillId)`, `getDependencies(skillId)`, `getReachable(skillId)`, `getImpactAnalysis(skillId)`, `getStats()` |
| 边类型 | `dependency`（依赖，权重1.0）, `blocking`（阻塞，权重0.8）, `semantic`（语义，权重0.5）, `causal`（因果，权重0.9） |
| 事件 | `node-added`, `edge-added`, `graph-built` |
| 依赖 | events, errors, debug-logger, shutdown-mixin |

#### SkillImprover — 技能改进器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/skill/skill-improver.js` |
| 功能 | 基于使用反馈持续迭代优化技能，记录学习条目(whatWorked/whatFailed/tips)，按技能ID聚合查询改进建议与避坑指南 |
| Bug修复 | `_onShutdown()` 从构造函数实例属性改为原型方法，修复与 `withShutdown` 混入的冲突 |

#### SkillImprovementLoop — 技能改进循环
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/skill/skill-improvement-loop.js` |
| 功能 | 技能改进的闭环反馈循环，支持跨Agent学习经验共享（CausalDataBus广播） |

#### SkillCreationEngine — 技能创建引擎
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/skill/skill-creation-engine.js` |
| 功能 | 自动化创建新Skill定义，支持AgentRuntime事件驱动的会话轨迹自动录制 |

#### SkillCurator — 技能策展器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/skill/skill-curator.js` |
| 功能 | Skill质量评估和推荐 |

#### SkillEvolver — 技能进化器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/skill/skill-evolver.js` |
| 功能 | LLM驱动的3阶段技能进化（总结→聚合→执行），可选启用，Token预算约束 |

#### SkillReducer — 动态技能管理系统
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/skill/skill-reducer.js` |
| 功能 | 三层缓存、动态分层（核心/领域/基础设施）、Top-K选择、任务驱动激活/卸载、过载检测、注意力压缩 |

### 1.8 存储与持久化

#### MemoryStore — JSON持久化存储
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/thought/memory-store.js` |
| 功能 | JSON文件持久化，键值存储，防抖写入 |

#### SqliteStore — SQLite存储
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/sqlite-store.js` |
| 功能 | 基于better-sqlite3的SQLite持久化，全文搜索，知识库管理 |
| Bug修复 | `getInstance()` 现在先缓存实例再调用 `init()`，若 `init()` 抛出异常则从缓存移除并关闭数据库句柄，防止部分初始化的实例被缓存；`_runSchemaMigrations()` 现在包含 `updated_at` 列的迁移，确保旧版数据库升级后不会因缺少列而报 SQLITE_ERROR |

#### CheckpointManager — 检查点管理器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/session/checkpoint-manager.js` |
| 功能 | 任务检查点创建、恢复、过期清理 |
| Bug修复 | `get()` 和 `list()` 现在返回检查点对象的防御性副本（数组字段使用 `.slice()`，元数据使用 `Object.assign()`），而非直接返回内部引用，防止外部修改污染内部状态 |

#### PlanPersistence — 计划持久化
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/workflow/plan-persistence.js` |
| 功能 | 执行计划的持久化存储和恢复 |
| Bug修复 | `createPlan()` 改用 `generateId('plan-')` 替代 `${sessionId}-${Date.now()}`，确保 planId 始终通过 `SAFE_PLAN_ID_RE` 验证 |

#### MCPClient — MCP协议客户端
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/mcp-client.js` |
| 功能 | MCP（Model Context Protocol）服务器连接、工具调用、命令白名单校验 |
| Bug修复 | 移除 `_toolCountByServer` 字段（此前计数包含未过滤的原始工具）；`getServerStatus()` 改用 `(this._serverToolIndex[name] || []).length` 获取准确工具数；`_pendingRequestCount` 递减现在使用 `Math.max(0, ...)` 防止并发清理路径导致负数计数 |
| OpenCLI集成 | 通过MCPClient的stdio传输模式连接OpenCLI MCP服务器（`npx -y @jackwener/opencli`），将80+网站和Electron应用转化为CLI命令。配置位于`.harness/config.json`的`mcp_servers.opencli`，默认`enabled: false`，`recommended: true`。需安装Chrome Bridge扩展实现浏览器会话复用。Dashboard API端点`/api/opencli/status`和`/api/opencli/servers`提供运行时状态查询，包含完整的防御性编码（null/typeof检查、try-catch异常保护、返回数据过滤） |

### 1.9 质量与推理

#### QualityScorer — 质量评分器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/quality/quality-scorer.js` |
| 功能 | 多维度质量评分，支持自定义权重 |

#### EntropyGovernanceOrchestrator — 熵治理编排器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/quality/entropy-governance-orchestrator.js` |
| 功能 | 融合Harness Engineering"持续熵治理"体系的核心编排器：聚合6大熵指标源（ContextDriftMonitor/ComprehensionDebtTracker/CodeDriftDetector/SkillReducer/AiCodeTrustScorer/DeliveryEfficiencyMeter），计算统一系统熵评分，实现"检测→告警→约束强化→验证"闭环和主动简化触发 |

#### ConvergenceDetector — 收敛检测器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/deepening/convergence-detector.js` |
| 继承 | `DeepeningBase` |
| 功能 | 迭代收敛检测，基于质量分数变化率、稳定性方差和最大迭代次数三重条件判断是否收敛，支持按执行ID隔离的独立历史追踪 |
| 关键方法 | `check(executionId, data)`, `getStats()` |
| 配置 | `qualityThreshold`（默认0.85）, `maxIterations`（默认10）, `minImprovementRate`（默认0.01）, `stabilityVariance`（默认0.01） |
| Bug修复 | `Math.max/min(...dimValues)` 替换为 `reduce()` 实现，防止大数组展开导致栈溢出；每次执行的历史数组新增上限控制（`maxIterations \|\| 50`），防止长期运行任务的历史无限增长 |
| 依赖 | DeepeningBase |

#### AdaptiveDepthController — 自适应深度控制器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/deepening/adaptive-depth-controller.js` |
| 继承 | `DeepeningBase` |
| 功能 | 根据任务复杂度自适应调整深化深度，评估任务属性（类型、依赖数、约束数、描述长度）计算复杂度分数，映射到对应的深化级别 |
| 关键方法 | `assessComplexity(task)`, `getStats()` |
| Bug修复 | 移除 `assessComplexity()` 中 `typeof task === 'string'` 分支的死代码——该分支在早期 `if (!task \|\| typeof task !== 'object')` 返回后不可达 |
| 依赖 | DeepeningBase |

#### FeedbackCredibility — 反馈可信度
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/quality/feedback-credibility.js` |
| 继承 | `EventEmitter` |
| 功能 | 反馈来源可信度评估系统，基于历史准确率计算来源信任评分，支持信任衰减（decayFactor），加权聚合多来源反馈，5维业务反思维度（business_impact/customer_satisfaction/cost_efficiency/compliance/feedback_quality） |
| 构造参数 | `config?: {decayFactor, defaultTrust}` |
| 关键方法 | `recordFeedback(sourceId, feedback, actualOutcome)`, `getCredibility(sourceId)`, `getWeightedScore(feedbacks)`, `getStats()` |
| 事件 | `feedback-recorded` |
| 依赖 | events |

#### SelfReflection — 自反思模块
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/quality/self-reflection.js` |
| 功能 | Agent执行结果自反思，生成改进建议 |

#### AdversarialReview — 对抗审查
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/quality/adversarial-review.js` |
| 功能 | 对Agent输出进行对抗性审查，发现潜在问题 |

#### IterativeRefinement — 迭代精炼
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/deepening/iterative-refinement.js` |
| 继承 | `DeepeningBase` |
| 功能 | 迭代精炼Agent输出，通过执行器-审查者循环逐步提升质量，每轮由审查者评估当前结果并反馈改进建议，执行器据此优化，直到达到质量阈值或最大精炼次数 |
| 关键方法 | `refine(executor, task, reviewer)`, `getStats()` |
| 配置 | `maxRefinements`（默认5）, `qualityThreshold`（默认0.8） |
| Bug修复 | `refine()` 使用 `Object.create(task)` 创建原型链副本替代直接修改输入 task 对象，避免副作用污染调用方数据 |
| 依赖 | DeepeningBase |

### 1.10 基础设施

#### EventBus — 事件总线
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/event-bus.js` |
| 功能 | 全局事件总线，发布/订阅模式，跨模块通信 |
| Bug修复 | `onceAsync()` 现在显式处理 `timeoutMs=0` 为"无超时"（通过 `noTimeout` 标志），将其与默认超时行为分离 |

#### HealthChecker — 健康检查器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/health-checker.js` |
| 功能 | 30+项健康检查注册，定期巡检，告警通知 |

#### TokenManager — Token管理器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/model/token-manager.js` |
| 功能 | Token预算管理，三层计数（输入/输出/工具调用），预算阈值预警 |

#### RetryEngine — 重试引擎
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/retry-engine.js` |
| 继承 | `EventEmitter` |
| 功能 | 指数退避重试策略，支持三级升级策略（replan→decompose→abort），withShutdown混入优雅关闭 |
| 关键方法 | `execute(task, depth)` — 执行任务并在失败时自动重试；`getAttempt(taskId)` — 获取任务重试上下文；`_onShutdown()` — 关闭时清理；`isHealthy()` — 健康检查（via withShutdown） |
| 配置选项 | `maxRetries`（默认3）, `backoffBaseMs`（默认1000）, 最大退避上限使用`DEFAULT_MAX_BACKOFF_MS` |
| 升级策略 | **replan**：重新执行任务（task.replan()返回新任务）；**decompose**：分解为子任务递归执行（最大深度3）；均耗尽后返回失败 |
| 事件 | `task-completed`, `task-retry`, `retry-exhausted`, `escalation` |
| Bug修复 | `_sleep(ms, shutdownGetter)` 从布尔值`shutdownFlag`改为getter函数`shutdownGetter`，休眠期间每500ms轮询检测关闭信号，避免关闭时仍长时间阻塞；升级replan循环同样使用getter函数检测关闭（此前缺失关闭检测） |

#### ConcurrencyController — 并发控制器
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/concurrency-controller.js` |
| 继承 | `EventEmitter` |
| 功能 | 并发任务控制，信号量机制，队列调度，超时保护，withShutdown混入优雅关闭 |
| 关键方法 | `acquire(taskId, metadata)` — 获取执行槽位（满则排队，超时返回false）；`release(taskId)` — 释放槽位并唤醒队列中下一个任务；`isRunning(taskId)` — 检查任务是否正在运行；`getRunningTasks()` — 获取所有运行中任务详情；`run(taskId, fn, metadata)` — acquire→execute→release一站式执行；`isHealthy()` — 健康检查；`getStats()` — 运行统计；`clear()` — 清空所有队列和运行状态 |
| 配置选项 | `maxConcurrent`（默认6）, `maxQueue`（默认100）, 超时使用`DEFAULT_ACQUIRE_TIMEOUT_MS` |
| 事件 | `acquired`, `released`, `queued`, `rejected`, `timeout`, `run-rejected` |
| Bug修复 | `release(taskId)` 使用`String(taskId)`进行Map查找，与`acquire()`行为一致（此前类型不匹配导致释放失败）；`acquire()` 新增队列重复检查`if (this._queue.some(e => e.taskId === safeTaskId)) return false`（此前同一taskId可重复入队）；`isRunning(taskId)` 使用`String(taskId)`进行Map查找（此前类型不匹配导致查询不准） |

#### CodeGraph — 代码仓库图谱
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/code-graph.js` |
| 继承 | `EventEmitter` |
| 功能 | 代码仓库依赖关系图谱，递归扫描目录解析JS文件require依赖和exports导出、MD文件Wiki链接和Markdown引用，构建文件级依赖图和反向依赖索引，支持模块路径解析和忽略模式配置 |
| 构造参数 | `options?: {maxDepth, maxFiles, ignorePatterns, fileExtensions}` |
| 关键方法 | `scanDirectory(dirPath, options?)`, `getDependencies(filePath)`, `getDependents(filePath)`, `getNode(filePath)`, `getStats()` |
| 边类型 | `require`（JS依赖）, `reference`（MD引用） |
| 事件 | `scan-complete`, `scan-error` |
| 依赖 | events, fs, path, debug-logger, shutdown-mixin |

#### ServiceFS — 服务文件系统
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/service-fs.js` |
| 继承 | `EventEmitter` |
| 功能 | 统一的服务文件系统抽象层，将不同后端服务挂载为虚拟文件系统路径（默认前缀`/services/`），通过适配器模式提供list/read/write/remove/exists五类操作，内置MemoryAdapter内存存储适配器，路径遍历防护，挂载数量限制 |
| 构造参数 | `config?: {mountPrefix, maxMounts, pathMaxLength}` |
| 关键方法 | `mount(serviceName, adapter)`, `unmount(serviceName)`, `resolve(path)`, `ls(path)`, `cat(path)`, `write(path, content)`, `rm(path)`, `exists(path)`, `getStats()` |
| 适配器方法 | `list`, `read`, `write`, `remove`, `exists` |
| 事件 | `mounted`, `unmounted`, `listed`, `read`, `written`, `deleted` |
| 依赖 | events, errors, debug-logger, shutdown-mixin |

#### OntologyFeedbackLoop — 本体反馈闭环
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/ontology-feedback-loop.js` |
| 功能 | 融合 Palantir Ontology "AI Actions" 核心机制，执行反馈闭环：行动→效果→模型优化自循环，自动调整规则阈值（基于滑动窗口统计），规则效果追踪与事件驱动 |

#### BusinessOntologyModel — 业务本体模型（升级）
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/business-ontology-model.js` |
| 功能 | 增强条件评估引擎：支持 AND/OR/NOT 复合逻辑、嵌套括号、时间窗口(:within)、聚合函数(count/sum/avg/min/max)、存在性(:exists/:empty)、包含(:contains)、范围(:between) |

#### SharedInfrastructure — 共享基础设施
| 项目 | 说明 |
|------|------|
| 文件 | `src/runtime/infrastructure/shared-infrastructure.js` |
| 功能 | 四个共享组件：SharedConnectionPool（连接池）、SharedLoadBalancer（负载均衡）、SharedServiceRegistry（服务注册）、SharedFeatureFlags（特性开关） |

#### 其他基础设施模块
| 模块 | 文件 | 功能 |
|------|------|------|
| StructuredIntent | `structured-intent.js` | 结构化意图解析、先验丰富度评分（assessPriorRichness）、Prompt质量报告（generateQualityReport） |
| WorkflowTemplate | `workflow-template.js` | 工作流模板 |
| PlatformCoordinator | `platform-coordinator.js` | 平台协调器 |
| AffinityLearner | `affinity-learner.js` | 亲和度学习 |
| UserModelManager | `user-model-manager.js` | 用户模型管理 |
| AutoVersionTracker | `auto-version-tracker.js` | 自动版本追踪；Bug修复：`_onShutdown()` 改为 async 并 await `waitForPendingWrites()`，确保关闭前 CHANGELOG.md 异步写入完成 |
| LTIContextInjector | `lti-context-injector.js` | LTI上下文注入；Bug修复：`getStats().totalInjections` 现在正确统计总注入事件数（所有历史缓冲区大小之和），而非仅统计已跟踪任务数；新增 `trackedTasks` 字段 |
| PhaseContextInjector | `phase-context-injector.js` | 阶段上下文注入；Bug修复：`injectForPhase()` 和 `injectForPhaseAsync()` 现在通过 `_cloneCachedResult()` 返回防御性副本，而非直接返回缓存引用，防止外部修改缓存数据 |
| IsolatedContextManager | `isolated-context-manager.js` | 隔离上下文管理；Bug修复：`getContext()` 现在返回 `result` 字段的防御性副本，而非直接返回内部引用，防止外部修改污染内部状态 |
| AutoregressiveContextSchema | `autoregressive-context-schema.js` | 自回归上下文模式；Bug修复：`MAX_STRING_LENGTH` 从 `DEFAULT_MAX_ENTRIES`（10000，条目数）改为专用常量2000（实际字符串长度限制）；移除未使用的 `DEFAULT_MAX_ENTRIES` 导入 |
| CausalMemoryStore | `causal-memory-store.js` | 因果记忆存储 |
| CausalDataBus | `causal-data-bus.js` | 因果数据总线 |
| ConfigCausalValidator | `causal-config-validator.js` | 配置因果验证 |
| RecurrentDeepeningScheduler | `recurrent-deepening-scheduler.js` | 循环深化调度 |
| MultiAgentRouter | `multi-agent-router.js` | 多Agent路由 |
| PluginManager | `plugin-manager.js` | 插件管理 |
| PriorityQueue | `priority-queue.js` | 优先级队列（堆实现）；Bug修复：`isHealthy()` 阈值从 `this._size < this._maxSize * 2`（恒为true，因size不超过maxSize）修正为 `this._size <= this._maxSize` |

---

## 二、门禁执行层（src/gate/，16个功能模块）

### TDDGate — TDD强制门禁
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/tdd-gate.js` |
| 功能 | 强制执行TDD的RED-GREEN-REFACTOR循环，阻止无测试的实现代码，验证覆盖率阈值（默认80%），检查抽象层级（文件>300行/新接口>3时警告），记录TDD周期 |
| 关键方法 | `check(context)`, `checkCoverage(coverage, threshold?)`, `recordCycle(taskId, phase, file)`, `validateCycleOrder(taskId)`, `enforceCheck(context)`, `enforceCoverage(context)`, `checkAbstractionLevel(context)` |
| 依赖 | errors (TDDGateError), bounded-array |

### EvidenceVerifier — 证据验证器
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/evidence-verifier.js` |
| 功能 | 验证Agent完成声明是否有足够证据支撑，22种Skill的证据需求映射，5维质量评估（完整性/特异性/一致性/可操作性/记忆-代码一致性），成功标准强度分类（强/弱），弱标准需人工确认，自反思提示生成 |
| 关键方法 | `verify(claim, evidence[], requiredTypes?, qualityCriteria?)`, `getRequiredEvidenceTypes(skillId)` |
| 依赖 | 无外部依赖 |

### FrameworkComplianceChecker — 框架合规检查器
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/framework-compliance-checker.js` |
| 继承 | `EventEmitter` |
| 功能 | 8大类合规检查：命名规范、结构规则、安全规则、持久化规则、API规则、错误规则、Karpathy规则、设计规则，支持豁免机制 |
| 关键方法 | `checkFile(filePath)`, `checkDirectory(dirPath)`, `checkProject()`, `checkNamingConvention(type, name)`, `getSummary()`, `addExemption(ruleId, filePath)` |
| 事件 | `file-checked`, `directory-checked`, `exemption-added`, `exemption-removed` |
| Bug修复 | `_results` 数组改用 `concat().slice()` 替代 `push(...violations)` + 条件 slice，防止 violations 数组较大时无限增长 |
| 依赖 | constants |

### DesignSkillEngine — 设计技能引擎
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/design-skill-engine.js` |
| 功能 | 前端设计系统引擎，设计反模式审计（纯黑/霓虹发光/AI渐变/大面积阴影等），自动修正（polish），设计Token系统（排版10级/间距4px网格/颜色3系统/动效6预设/响应式5断点/组件6类），无障碍检查（WCAG对比度），5种公司设计语言参考 |
| 关键方法 | `audit(source, type)`, `polish(source)`, `normalize(source)`, `auditAccessibility(source)`, `checkContrast(fg, bg)`, `critique(source, focusArea?)`, `getTypographyScale()`, `getSpacingScale()`, `getColorSystem(name?)`, `getMotionPreset(name?)`, `getComponentTokens(component?)`, `generateDesignMd(options?)`, `generateResponsiveCSS()`, `generateAccessibilityCSS()` |
| Bug修复 | `_parseColor()` 现在正确验证十六进制颜色——支持3/4/6/8位hex，无效格式返回null；`_relativeLuminance()` 现在要求 `#` 前缀才处理hex，防止普通字符串的误匹配；`audit()` 现在每次调用创建新 RegExp 实例而非复用带 `lastIndex` 状态的共享模式 |
| 依赖 | constants |

### GeneratorVerifier — 生成器验证器
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/generator-verifier.js` |
| 继承 | `EventEmitter` |
| 功能 | 验证AI生成输出的正确性，5维度加权评分（逻辑正确性0.30/需求对齐0.25/边界覆盖0.20/一致性0.15/完整性0.10），验证-修正循环 |
| 关键方法 | `verifyCorrectness(context)`, `createVerificationLoop(context)`, `executeVerificationLoop(loop, generateFn?, verifyFn?)`, `getVerificationHistory(skillId?, limit?)` |
| Bug修复 | 矛盾检测模式现在排除合法短语如"必须确保不能遗漏"和"must verify that cannot omit"，减少误报 |
| 事件 | `verification-complete`, `shutdown` |
| 依赖 | events |

### DeviationApproval — 偏差审批
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/deviation-approval.js` |
| 继承 | `EventEmitter` |
| 功能 | 框架合规偏差的审批流程管理，TTL过期机制（默认30天），持久化到`.harness/deviations/`，最大200条偏差记录 |
| 关键方法 | `request(data)`, `approve(deviationId, reviewer)`, `reject(deviationId, reviewer)`, `revoke(deviationId, revoker)`, `isApproved(ruleId, filePath)`, `getPending()`, `getApproved()` |
| 偏差状态 | PENDING → APPROVED / REJECTED → EXPIRED / REVOKED |
| 事件 | `deviation-requested`, `deviation-approved`, `deviation-rejected`, `deviation-revoked`, `deviation-expired` |
| Bug修复 | `_isSafeFilePath()` 现在在 `isPathWithinDir` 检查前将相对路径解析为绝对路径 |
| 依赖 | errors, constants, debounced-persister, debug-logger |

### SkillPatchApproval — 技能补丁审批
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/skill-patch-approval.js` |
| 继承 | `EventEmitter` |
| 功能 | 技能修改审批流程管理，影响评估，防止未经审核的Skill定义变更 |
| Bug修复 | `_evictOldest()` 现在处理 NaN 时间戳，给予其最高驱逐优先级 |

### CodeReviewFrameworkCheck — 代码审查框架检查
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/code-review-framework-check.js` |
| 继承 | `EventEmitter` |
| 功能 | 基于框架合规规则的代码审查系统，9类审查类别，8项自动检查，审查生命周期管理，持久化到`.harness/reviews/` |
| 关键方法 | `createReview(data)`, `runChecklist(reviewId)`, `approveReview(reviewId, approver)`, `requestChanges(reviewId, requester)`, `rejectReview(reviewId, rejecter)` |
| 审查状态 | PENDING → IN_PROGRESS → APPROVED / REJECTED / NEEDS_CHANGES |
| 事件 | `review-created`, `review-completed`, `review-approved`, `review-needs-changes`, `review-rejected` |
| 依赖 | errors, constants, debounced-persister, debug-logger |

### KarpathyEnhancer — Karpathy增强器
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/karpathy-enhancer.js` |
| 继承 | `EventEmitter` |
| 功能 | Karpathy编码原则增强器，在现有规则基础上注入四项工程纪律规则：Diff卫生（diff-hygiene，无关联变更检测）、返工率（rework-rate，同区域反复修改检测）、澄清优先（clarification-first，编码前澄清比例）、孤立代码率（orphan-code-rate，无调用者代码检测），支持有效性度量 |
| 构造参数 | `options?: {diffHygieneThreshold, maxReworkCount, minClarificationRate, maxOrphanRate}` |
| 关键方法 | `enhance(existingRules)`, `measureEffectiveness(metrics)`, `getStats()` |
| 增强规则 | `diff-hygiene`（Diff卫生）, `rework-rate`（返工率）, `clarification-first`（澄清优先）, `orphan-code-rate`（孤立代码率） |
| 事件 | `rules-enhanced`, `effectiveness-measured` |
| 依赖 | events, errors, safe-assign, debug-logger, shutdown-mixin |

### SharedRuleHelpers — 共享规则辅助
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/shared-rule-helpers.js` |
| 功能 | 框架合规检查的共享规则辅助工具集，提供代码内容预处理（注释和字符串剥离）、安全模式检测（eval/Function、Math.random/Date.now作为加密种子）、命名规范验证、文件结构验证等通用检查能力，供 `FrameworkComplianceChecker`、`CodeReviewFrameworkCheck` 等多个门禁模块复用 |
| 关键方法 | `stripCommentsAndStrings(content)`, `checkNoEval(content)`, `checkCryptoSafe(content)`, `checkNamingConvention(name, convention)`, `checkFileStructure(filePath, rules)` |
| Bug修复 | 从 `stripCommentsAndStrings()` 中移除了 `STRIP_REGEX_RE`（`/\/[^/]*\//g`），该正则表达式会错误匹配除法运算符和URL路径，导致安全检查（eval检测、Math.random检测）产生假阴性——恶意代码隐藏在 `a/b/c` 形式的表达式中会被误剥离而逃过检测 |
| 依赖 | 无外部依赖 |

### LayerBoundaryGuard — 层级边界守卫
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/layer-boundary-guard.js` |
| 功能 | 四层架构（Interaction/Business/Domain/Infrastructure）依赖方向强制执行，检测跨层违规引用，支持strict/recommended/optional三级严格度 |

### ArchitectureBoundaryEnforcer — 架构边界执行器
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/architecture-boundary-enforcer.js` |
| 功能 | 模块间依赖方向约束矩阵，定义允许的依赖方向和禁止的跨模块引用，strict/recommended/optional分级执行 |

### CodeDriftDetector — 代码漂移检测器
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/code-drift-detector.js` |
| 功能 | 跨时间架构偏离追踪，检测违规增长趋势、耦合度变化、层失衡，输出漂移报告和告警 |

### DesignTokens — 设计令牌
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/design-tokens.js` |
| 功能 | 设计令牌定义与CSS变量生成，支持主题配置、颜色/间距/字体/动画令牌管理 |

### ProductDefinitionGate — 产品定义门禁
| 项目 | 说明 |
|------|------|
| 文件 | `src/gate/product-definition-gate.js` |
| 功能 | 产品定义质量门禁：校验提案必填字段、目标与非目标重叠检测、MVP范围约束、可行性成本审查、原型污染防护 |

---

## 三、安全权限层（src/permission/，3个文件）

### RBACEnforcer — 角色访问控制执行器
| 项目 | 说明 |
|------|------|
| 文件 | `src/permission/rbac-enforcer.js` |
| 功能 | 从`.harness/agents/`和`.harness/skills/`加载权限定义，验证Agent是否有权执行Skill，强制执行strict/recommended/optional分级，验证Skill执行顺序依赖 |
| 关键方法 | `load()`, `canExecute(agentId, skillId)`, `isStrict(skillId)`, `validateExecutionOrder(skillId, completedSkills)`, `enforceExecute(agentId, skillId)`, `getAgentPermissions(agentId)`, `getAgentModel(agentId)` |
| Bug修复 | `_loadDefinitionsFromDir()` 现在统一使用 `_addLoadError()` 而非直接 `this._loadErrors.push()`，确保 `MAX_LOAD_ERRORS` 限制被正确遵守 |
| 依赖 | constants, debug-logger, errors (PermissionError) |

### PermissionGuard — 文件权限守卫
| 项目 | 说明 |
|------|------|
| 文件 | `src/permission/permission-guard.js` |
| 继承 | `EventEmitter` |
| 功能 | 阻止项目外文件操作，保护.harness系统文件，文件级锁（5分钟超时），13种危险命令模式检测，路径遍历防护，用户确认机制（5分钟过期） |
| 关键方法 | `checkFileWrite(filePath, agentId)`, `checkFileDelete(filePath, agentId)`, `checkFileRead(filePath, agentId)`, `checkCommand(command, agentId)`, `acquireLock(filePath, agentId)`, `releaseLock(filePath, agentId)`, `recordConfirmation(agentId, action, target)`, `enforceFileWrite(filePath, agentId)`, `enforceCommand(command, agentId)` |
| 事件 | `confirmation-recorded` |
| 依赖 | constants, debug-logger, errors (PermissionError) |

### AuditLogger — 审计日志
| 项目 | 说明 |
|------|------|
| 文件 | `src/permission/audit-logger.js` |
| 继承 | `EventEmitter` |
| 功能 | 记录所有权限检查事件，链式哈希保证日志完整性（SHA-256区块链式防篡改），持久化到`.harness/audit/`，最大10000条记录 |
| 关键方法 | `log(agent, action, target, result, reason?, details?)`, `verifyIntegrity()`, `query(filter)`, `getRecent(count?)`, `getStats()`, `export()` |
| 事件 | `shutdown` |
| Bug修复 | `Math.min(...tamperedIndices)` 替换为 `reduce()` 模式，防止大数组展开导致 RangeError 栈溢出 |
| 依赖 | constants, bounded-array, debounced-persister, debug-logger |

---

## 四、Web服务层（src/web/，30+个文件）

### DashboardServer — 仪表盘服务器
| 项目 | 说明 |
|------|------|
| 文件 | `src/web/server.js` |
| 功能 | HTTP服务器+WebSocket，220+ API端点，安全认证（timingSafeEqual），CORS，gzip压缩，API缓存，设计系统审计，Agent全景图 |
| 关键方法 | `start(callback)`, `stop()`, `invalidateCache(key)`, `isHealthy()` |
| API分类 | 核心仪表盘(14) / 框架信息(3) / 合规检查(2) / 设计系统(14) / Agent管理(22) / 深化子系统(46) / 子Agent(4) / 协作模式(11) / 技能管理(18) / 存储记忆(12) / 目标执行(7) / 审批管理(6) / 文档新鲜度(5) / 运行时基础设施(16) / OpenCLI(2) / 其他(30+) |

### WebSocketHandler — WebSocket处理器
| 项目 | 说明 |
|------|------|
| 文件 | `src/web/websocket-handler.js` |
| 继承 | `EventEmitter` |
| 功能 | RFC 6455 WebSocket升级处理，多方式认证（Bearer Header / Sec-WebSocket-Protocol / URL query已禁用），timingSafeEqual令牌比对，来源验证（白名单或Host匹配），连接数限制，心跳检测，消息限流，帧缓冲超时检测，最大负载限制 |
| 关键方法 | `handleUpgrade(req, socket, head)` — 处理WebSocket升级握手；`broadcast(event, data)` — 向所有活跃客户端广播消息（自动清理死连接）；`close()` — 优雅关闭所有连接；`shutdown()` — 关闭并移除所有监听器；`isHealthy()` — 健康检查（客户端数<MAX_CLIENTS） |
| 事件 | `connection`, `message`, `disconnect` |
| 常量 | `WS_MESSAGE_RATE_LIMIT=30`, `WS_MESSAGE_RATE_WINDOW=1000ms`, `HEARTBEAT_INTERVAL=30s`, `MAX_PAYLOAD_SIZE=1MB`, `MAX_FRAME_BUFFER_SIZE=1MB`, `MAX_CLIENTS=50` |
| 安全 | Server.start()传入`authToken: process.env.HARNESS_API_TOKEN \|\| null`；生产环境无token拒绝连接；非本地无token拒绝连接；URL query参数传token已禁用（返回400） |
| Bug修复 | **限流修复**：`_checkMessageRate`改为先过滤窗口外时间戳再检查数量（此前先截断到`WS_MESSAGE_RATE_LIMIT`再检查，导致限流可被绕过）；**心跳修复**：帧缓冲超时检测从`DEFAULT_MAX_BACKOFF_MS`（重试常量）改为`HEARTBEAT_INTERVAL * 2`（60s），语义正确；**广播死连接处理**：广播写入失败的客户端现在会销毁socket并触发`disconnect`事件（此前仅从Set中删除）；**认证修复**：`_validateWsAuth`长度不匹配时写入401响应并销毁socket |

### 前端文件
| 文件 | 功能 |
|------|------|
| `public/index.html` | PWA入口页面 |
| `public/app.js` | 前端应用逻辑，WebSocket连接，XSS防护，HTML消毒，自动重连 |
| `public/styles.css` | 样式表，设计系统变量 |
| `public/sw.js` | Service Worker，离线缓存 |
| `public/manifest.webmanifest` | PWA清单文件 |

---

## 五、工具层（src/utils/，29个功能模块）

| 模块 | 文件 | 功能 |
|------|------|------|
| Constants | `constants.js` | 全局常量（阶段/转换/内置模块/优先级/DANGEROUS_KEYS），验证工具函数（parseFrontmatter/parseArray/validateAgentId/validateProjectRoot/validateString/validateNumber/validateObject/validateFunction/validateEnum/sanitizeString/generateId/estimateTokens/isNonEmptyString） |
| Sanitizer | `sanitizer.js` | 安全清理工具集（sanitizeProto/sanitizeObject/sanitizeFilePath/sanitizeLogMsg/sanitizeMcpEnv），统一原型污染防护、路径清理、日志脱敏、环境变量过滤，替代散布各文件的14个_sanitize*局部函数 |
| SafeAssign | `safe-assign.js` | 安全对象合并（原型污染防护），替代 `Object.assign`，过滤 `DANGEROUS_KEYS`中的12个危险键 |
| SafeParse | `safe-parse.js` | 安全JSON解析，防止异常抛出，支持默认值回退 |
| SafeExecute | `safe-execute.js` | 安全执行工具集（safeCall/safeExecute/emitError/errorMessage/clamp01/roundTo），异常捕获与默认值 |
| PathUtils | `path-utils.js` | 路径安全工具（`validatePath`/`isPathWithinDir`/`sanitizePath`），统一路径遍历防护，null byte检测 + realpath + 规范化前缀匹配 |
| NetworkUtils | `network-utils.js` | 网络检测工具（isLocalAddress/isPrivateIP），请求来源验证 |
| ParamValidator | `param-validator.js` | 参数校验工具，统一验证函数参数类型和范围；含`ensureSafeTimeout`钳位定时器延迟到[1, 2^31-1]防止整数溢出 |
| ConfigValidator | `config-validator.js` | 配置验证（版本/enforcement/运行时参数/安全扫描），脱敏处理 |
| StructuredLogger | `structured-logger.js` | 结构化日志（4级别/性能日志/操作日志/子Logger/traceId/查询/导出），最大5000条 |
| DebugLogger | `debug-logger.js` | 条件调试输出（HARNESS_DEBUG环境变量），StructuredLogger桥接 |
| LRUCache | `lru-cache.js` | LRU缓存（Map实现，可配置容量） |
| TTLCache | `ttl-cache.js` | TTL过期缓存，自动清理过期条目 |
| BoundedArray | `bounded-array.js` | 有界数组（可配置容量，支持onEvict回调，完整数组API） |
| BoundedMap | `bounded-map.js` | 有界Map，容量限制与自动淘汰 |
| RingBuffer | `ring-buffer.js` | 环形缓冲区（FIFO，可配置容量，支持迭代器） |
| MinHeap | `min-heap.js` | 最小堆数据结构，O(log n)插入和提取 |
| DebouncedPersister | `debounced-persister.js` | 防抖持久化（可配置延迟，原子写入，优雅关闭） |
| KeyedDebouncer | `keyed-debouncer.js` | 键控防抖器，按键独立防抖，批量刷新 |
| JsonStoreRestorer | `json-store-restorer.js` | JSON文件加载/保存，损坏检测与自动修复，类型验证 |
| FsUtils | `fs-utils.js` | 文件系统工具（ensureDir/readJsonDir/loadJson/原子操作），路径安全 |
| UniqueId | `unique-id.js` | ID生成器（timestampId/secureId/ID_PREFIXES），唯一性保证 |
| StableStringify | `stable-stringify.js` | 稳定JSON序列化，键排序确保输出一致性 |
| DeepClone | `deep-clone.js` | 深拷贝工具，支持循环引用检测 |
| StateCompare | `state-compare.js` | 状态比较工具，深度差异检测 |
| ShutdownMixin | `shutdown-mixin.js` | 优雅关闭混入（withShutdown），统一shutdown生命周期管理 |
| I18n | `i18n.js` | 国际化支持（t函数/setLocale/getLocale） |
| CapacityConfig | `capacity-config.js` | 容量配置管理；Bug修复：`loadCapacityConfig()` 现在返回 `Object.freeze({..._cachedConfig})` 替代可变缓存引用，防止外部修改污染缓存 |

---

## 六、错误体系（src/errors/，1个文件）

| 错误类 | 父类 | HTTP状态码 | 用途 |
|--------|------|-----------|------|
| HarnessError | Error | 可配置 | 基础错误（code/severity/httpStatus/errorId/timestamp/context/isRetryable） |
| SessionError | HarnessError | 400 | 会话错误 |
| PermissionError | HarnessError | 403 | 权限错误 |
| TDDGateError | HarnessError | 403 | TDD门禁错误 |
| AgentError | HarnessError | 500 | Agent错误 |
| DeepeningError | HarnessError | 500 | 深化子系统错误 |
| PipelineError | HarnessError | 500 | 管道错误 |
| CausalViolationError | HarnessError | 409 | 因果一致性违规错误 |
| HookError | HarnessError | 500 | Hook错误 |

错误码体系：72个错误码，涵盖UNKNOWN/INIT/CONFIG/SESSION/PERMISSION/TDD/EVIDENCE/AGENT/SKILL/PIPELINE/DEEPENING/HOOK/COMMAND/STORAGE/VALIDATION/TIMEOUT/SHUTDOWN/RESOURCE/CAPACITY/CONNECTION/SECURITY/DEPENDENCY/PLUGIN/SNAPSHOT/AUDIT/RETRY/LOCK/LOAD_BALANCER/DATA_PIPELINE/NOTIFIER/EVENT_BUS/STATE_MACHINE/TASK_SCHEDULER/METRICS/BACKPRESSURE/PRIORITY_QUEUE/SNAPSHOT_STORE/CAUSAL/MCP等类别。

---

## 七、主入口（src/index.js）

### create(projectRoot, options?) — 框架工厂函数
创建并返回完整的框架实例，初始化90+个模块实例，按10个批次顺序初始化：
1. 配置验证 → validateConfig
2. 核心模块 → 26个（SkillRouter, SessionManager, PhaseOrchestrator, RBACEnforcer等）
3. 路由和Hook → 4个
4. 思维模块 → 6个
5. 存储模块 → 7个
6. Agent模块 → 7个
7. 深化模块 → 6个
8. 协作模块 → 5个
9. 目标模块 → 5个
10. 事件注册 → 健康检查(30+项)、审计中间件、会话事件

### 实例API分组
| 分组 | 包含模块 |
|------|---------|
| core | router, session, orchestrator, enforcer, guard, logger, tddGate, verifier, eventBus, healthChecker, tokenManager, commandRouter, programmableHookExecutor, contextCompressionEngine |
| agents | runtime, lifecycle, sandbox, monitor, deployment, stateManager, workflowIntegration, packManager |
| gate | tddGate, verifier, complianceChecker, deviationApproval, codeReviewCheck, designSkillEngine, generatorVerifier |
| collaboration | modeRouter, structuredIntent, subagentExecutor, pairChat, chatChain |
| store | sqlite, memory, improvementLoop, nudge, creationEngine, curator, userModelManager, mcpClient |

### 核心执行管线
```
用户请求 → CommandRouter(命令解析) → SkillRouter(技能匹配)
    → PhaseOrchestrator(阶段验证) → RBACEnforcer(权限检查)
    → ProgrammableHookExecutor(前置Hook) → SubagentExecutor(子Agent执行)
    → GoalExecutor(目标迭代) / CollaborationModeRouter(协作模式)
    → DeepeningOrchestrator(深化推理) → QualityScorer(质量评分)
    → EvidenceVerifier(证据验证) → TDDGate(TDD门禁)
    → ProgrammableHookExecutor(后置Hook) → 结果返回
```

---

## 八、配置与定义文件

### .harness/agents/（25个Agent定义）
| 类型 | Agent | 文件 |
|------|-------|------|
| 职能型 | team-lead | `team-lead.md` |
| 职能型 | domain-analyst | `domain-analyst.md` |
| 职能型 | task-worker | `task-worker.md` |
| 职能型 | quality-assurance | `quality-assurance.md` |
| 职能型 | devops-engineer | `devops-engineer.md` |
| 职能型 | technical-writer | `technical-writer.md` |
| 任务型 | code-reviewer | `code-reviewer.md` |
| 任务型 | security-reviewer | `security-reviewer.md` |
| 任务型 | build-error-solver | `build-error-solver.md` |
| 任务型 | planner | `planner.md` |
| 任务型 | test-writer | `test-writer.md` |
| 语言审查 | typescript-reviewer | `typescript-reviewer.md` |
| 语言审查 | python-reviewer | `python-reviewer.md` |
| 语言审查 | rust-reviewer | `rust-reviewer.md` |
| 语言审查 | java-reviewer | `java-reviewer.md` |
| 语言审查 | go-reviewer | `go-reviewer.md` |
| Codex方法论 | system-designer | `system-designer.md` |

### .harness/skills/（84个Skill定义）
| 阶段 | Skill | 强制级别 |
|------|-------|---------|
| 需求探索 | brainstorming, idea-validation, decision-loop | recommended |
| 需求分析 | requirement-analysis, necessity-review | recommended |
| 架构设计 | design-md, architecture-design, taste-skill, cloud-ai-blueprint | strict/recommended |
| 模块开发 | tdd-implement, module-development, impeccable, ui-skills, iterative-deepening, multi-agent-fusion, motion-ai-kit, better-icons, web-interaction, ai-prompting, mvp-builder, dispatching-parallel | strict/optional |
| 质量保障 | code-review, verification-before-completion, systematic-debugging, bug-fix, security-audit, performance-optimization, refactor-code | strict/recommended |
| 集成测试 | integration-testing | strict |
| 部署上线 | deployment, documentation, auto-doc-generation, ai-native-scaling | strict/recommended |
| 扩展 | writing-skills | optional |
| 基础设施 | skill-router, session-start-hook | - |

### .harness/commands/（24个斜杠命令）
| 命令 | 技能链 |
|------|--------|
| /plan | brainstorming → requirement-analysis → architecture-design |
| /code-review | code-review |
| /security-review | security-audit |
| /debug | systematic-debugging → bug-fix |
| /fix | bug-fix → verification-before-completion |
| /deploy | verification-before-completion → deployment |
| /test | integration-testing |
| /refactor | refactor-code |
| /prompt | ai-prompting |
| /audit | security-audit |
| /build | tdd-implement → module-development |
| /code-simplify | refactor-code |
| /goal | iterative-deepening → brainstorming → requirement-analysis |
| /learn | iterative-deepening |
| /optimize | performance-optimization |
| /review | code-review → security-audit |
| /section | ui-skills → design-md |
| /ship | verification-before-completion → deployment |
| /spec | requirement-analysis → architecture-design |
| /startup | brainstorming → idea-validation → requirement-analysis → architecture-design → mvp-builder → deployment → ai-native-scaling |
| /decide | decision-loop |
| /web | web-interaction |
| /cli | cli-anything |

### .harness/rules/（16个全局规则）
| 规则文件 | 覆盖内容 |
|---------|---------|
| task-execution.md | 任务拆分、交付物、验收标准 |
| context-management.md | 上下文加载、Skill按需加载 |
| document-standards.md | 六层文档体系、命名规范 |
| monitoring-fault-tolerance.md | 超时、重试、Checkpoint |
| cost-control.md | Token预算、预警阈值 |
| security-permissions.md | 敏感信息、权限检查 |
| best-practices.md | 上下文过长/工具失败/任务超时等处理 |
| memory-persistence.md | 记忆持久化策略 |
| karpathy-principles.md | Karpathy编码原则 |
| coding-standards.md | 编码规范 |
| ai-prompting-standards.md | AI提示词规范、防幻觉策略 |
| anti-sycophancy.md | 反谄媚规则、独立判断保障 |
| anti-bad-code.md | 反劣质代码规则 |
| token-efficiency.md | Token效率规则 |

---

## 关联文档
- [架构分析-AIProject系统](../architecture/架构分析-AIProject系统.md)
- [接口文档-Web API](../tools/接口文档-Web API.md)
- [开发指南-代码贡献规范](../guidelines/开发指南-代码贡献规范.md)
- [核心功能-多Agent协作流程](../core/核心功能-多Agent协作流程.md)
- [深度拆解-任务调度执行链路](../deep-dive/深度拆解-任务调度执行链路.md)
